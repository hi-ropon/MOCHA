ファイル名: HM-60252J_page73

<overview>
本ページは「ラウンドFWD方向絶対位置決め」に関する説明で構成されています。テキスト部分では機能の概要と使用例、運転方法の手順が記載されています。表部分ではラウンド機能の設定値と運転データの具体的な数値が示されています。グラフ部分には座標イメージの円グラフと運転イメージの速度-時間グラフ、さらにタイミングチャートが掲載されています。
</overview>

<contents>
### テキスト
#### ラウンドFWD方向絶対位置決め
運転データにラウンド範囲内の目標位置を設定し、目標位置に関わらず常にFWD方向へ位置決めSD運転を実行します。

##### 使用例
指令位置100の地点から目標位置8,600へ運転する場合  
（ラウンド設定範囲18 rev.、ラウンドオフセット比率50％）

##### 運転方法
1. READY出力がONであることを確認します。  
2. M0～M7入力で運転データNo.を選択し、START入力をONにします。  
3. READY出力がOFFになり、モーターが運転を開始します。  
4. READY出力がOFFになっていることを確認し、START入力をOFFにします。  
5. 運転が終わると、READY出力がONになります。

### 表
#### ラウンド機能の設定

| 項目 | 設定値 |
| :--- | :--- |
| 初期原点生成・ラウンド座標設定範囲 [rev] | 18.0 |
| 初期原点生成・ラウンドオフセット比率設定 [%] | 50.00 |
| 初期原点生成・ラウンドオフセット値設定 [step] | 0 |
| ラウンド(RND)設定 | 有効 |
| RND-ZERO出力用RND分割数 | 1 |

#### 運転データの設定

| No. | 名前 | 方式 | 位置 [step] | 速度 [Hz] | 起動・変速レート [kHz/s] | 停止レート [kHz/s] |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 0 | - | ラウンドFWD方向絶対位置決め | 8600 | 2000 | 1.500 | 1.500 |

### 図
#### 座標イメージ
```mermaid
flowchart LR
    A[中心点] --> B[0]
    A --> C[100]
    A --> D[4,500]
    A --> E[-4,500]
    A --> F[-9,000]
    A --> G[8,600]
```
* 円形座標上に0、100、±4,500、-9,000、8,600の位置を示す。

#### 運転イメージ（速度-時間グラフ）
```mermaid
flowchart TD
    Start((START))
    Start -->|0→500Hz 1.5s| SpeedUp[速度上昇]
    SpeedUp -->|2000Hz 維持| ConstantSpeed[定速運転]
    ConstantSpeed -->|500Hz 1.5s| SpeedDown[速度減速]
    SpeedDown --> End((終了))
```
* 速度は0から500Hzへ1.5秒で上昇し、2000Hzで一定時間運転後、500Hzへ1.5秒で減速。

#### タイミングチャート
```mermaid
sequenceDiagram
    participant START入力
    participant M0～M7入力
    participant READY出力
    participant 内部速度指令

    M0～M7入力->>START入力: ON (①)
    START入力->>READY出力: OFF (②)
    READY出力->>内部速度指令: 運転開始 (③)
    START入力->>READY出力: OFF解除 (④)
    内部速度指令->>READY出力: 運転終了 (⑤)
```
* START入力、M0～M7入力、READY出力、内部速度指令の状態変化を示す。

</contents>