ファイル名: HM-60252J_page224

<overview>
本ページは「動力遮断機能」に関する説明で構成されています。テキスト部分ではEDM-MON出力の動作説明、HWTO入力検出のアラーム動作について記載されています。表部分では関連するパラメータID、名称、内容、初期値が整理されています。グラフ部分はHWTO-2重系異常検出遅延時間の動作タイミングを示す図があり、HWTO1入力およびHWTO2入力のON/OFF状態とアラーム発生の関係を視覚的に表現しています。
</overview>

<contents>
### テキスト
#### 動力遮断機能

- **EDM-MON出力**  
  HWTO1入力とHWTO2入力の両方がOFFになると、EDM-MON出力がONになります。  
  **重要**  
  動力遮断機能の故障検出には、必ずCN1のEDM出力を使用してください。

- **HWTO入力検出のアラーム**  
  「HWTO動作」パラメータを「1：アラーム発生あり」に設定すると、HWTO1入力またはHWTO2入力をOFFにしたときにアラームが発生します。（アラームコード68h）  
  このとき、PWR/ALM LED（またはPOWER/ALARM LED）が赤色に1回点滅を繰り返します。  
  「HWTO動作」パラメータを「1：アラーム発生あり」に設定した場合、動力遮断機能を解除した後にALM-RST入力をOFFからONにすると、モーターを励磁できます。（ONエッジで有効）

- **HWTO入力回路異常のアラーム**  
  HWTO1入力またはHWTO2入力の片方がOFFになってから、もう片方の入力がOFFになるまでの時間が「HWTO-2重系異常検出遅延時間」パラメータの設定値を超えるとアラームが発生します。（アラームコード53h）  
  このとき、PWR/ALM LED（またはPOWER/ALARM LED）が赤色に2回点滅を繰り返します。

### 表
#### 関連するパラメータ（HWTO動作のアラーム）
| パラメータID | 名称       | 内容                                                                                     | 初期値 |
| :----------- | :--------- | :--------------------------------------------------------------------------------------- | :----- |
| 400 (0190h)  | HWTO動作   | HWTO1入力またはHWTO2入力をOFFにしたときにアラームを発生させます。<br>【設定範囲】0:アラーム発生なし 1:アラーム発生あり | 0      |

#### 関連するパラメータ（HWTO入力回路異常のアラーム）
| パラメータID | 名称                     | 内容                                                                                                               | 初期値 |
| :----------- | :----------------------- | :----------------------------------------------------------------------------------------------------------------- | :----- |
| 401 (0191h)  | HWTO-2重系異常検出遅延時間 | HWTO1入力またはHWTO2入力の片方がOFFになってから、もう片方の入力がOFFになるまでの時間がこのパラメータの設定値を超えるとアラームが発生します。<br>【設定範囲】0～10（無効）、11～100 ms | 0      |

### 図
```mermaid
flowchart LR
    subgraph HWTO1入力
        A1[ON] --> A2[OFF]
    end
    subgraph HWTO2入力
        B1[ON] --> B2[OFF]
    end

    A2 -->|HWTO-2重系異常検出遅延時間| C1[アラームは発生しない]
    B2 --> C1

    A2 -->|HWTO-2重系異常検出遅延時間| C2[アラームが発生する]
    B2 --> C2
```

**図の説明:**  
- HWTO1入力とHWTO2入力のON/OFF状態の組み合わせにより、アラームの発生有無が決まる。  
- 片方の入力がOFFになってからもう片方の入力がOFFになるまでの時間が遅延時間を超えるとアラームが発生する。  
</contents>