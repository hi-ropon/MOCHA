ファイル名: HM-60252J_page83

<overview>
このページは「連続運転（トルク）」に関する説明で構成されています。  
テキスト部分では連続運転の概要と使用例、運転方法の手順が記載されています。  
表部分には運転データの設定例が示されています。  
グラフ部分には速度と位置・時間の関係を示す運転イメージの図と、信号のON/OFF状態を示すタイミングチャートが含まれています。
</overview>

<contents>
### テキスト
#### 連続運転（トルク）
運転データに設定した速度で自動運転を行わず、速度を維持したまま運転を続けます。負荷に押し当たったときは、負荷に対して加圧し続けます。

##### 使用例
運転データの設定例が示されています。

##### 運転方法
1. READY出力がONであることを確認します。  
2. M0～M7入力で運転データNo.を選択し、START入力をONにします。READY出力がOFFになり、モーターが運転を開始します。  
3. READY出力がOFFになっていることを確認し、START入力をOFFにします。  
4. STOP入力をONにします。モーターが即停止します。  
5. モーターが停止すると、READY出力がONになります。

### 表
#### 運転データの設定例

| 名前  | 方式           | 位置 [step] | 速度 [Hz] | 起動・変速レート [kHz/s] | 停止レート [kHz/s] | 運転電流 [%] |
| :---- | :------------- | :---------- | :-------- | :----------------------- | :----------------- | :----------- |
| No.0  | 連続運転(トルク) | 0           | 2000      | 1000.000                 | 1000.000           | 20.0         |

### 図
#### 運転イメージ（速度の変化）

```mermaid
flowchart LR
    subgraph 速度-位置グラフ
        A[速度] --> B[2000]
        B --> C[一定速度維持]
        C --> D[位置]
    end

    subgraph 速度-時間グラフ
        E[速度] --> F[2000]
        F --> G[一定速度維持]
        G --> H[時間]
    end
```

#### タイミングチャート（信号のON/OFF状態）

```mermaid
flowchart TD
    START入力 -->|ON| ②
    ② --> ③
    M0～M7入力 -->|ON| ③
    STOP入力 -->|ON| ④
    READY出力 -->|ON| ①
    ③ -->|READY出力 OFF| ⑤
    ④ -->|モーター停止| ⑤
```

**図の説明:**  
- START入力、M0～M7入力、STOP入力、READY出力のON/OFF状態を時間軸に沿って示し、モーターの運転開始から停止までの流れを表現しています。  
- 内部速度指令はSTART入力ON後に出力され、STOP入力ONで停止します。
</contents>