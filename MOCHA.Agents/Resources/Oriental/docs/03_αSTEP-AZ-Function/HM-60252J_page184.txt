ファイル名: HM-60252J_page184

<overview>
本ページは「STOP・STOP-COFF入力停止方法が『減速停止』の場合」の動作説明に関する内容で構成されています。  
テキスト部分では、STOP-COFF入力がON/OFFになった際のモーターの動作や出力信号の変化について説明しています。  
グラフ部分には、各信号（STOP-COFF入力、PLS-RDY出力、READY出力、MOVE出力、モーター動作、モーター励磁、電磁ブレーキ）の時間変化を示すタイミングチャートが2種類掲載されています。  
表はなく、図はタイミングチャートのみです。
</overview>

<contents>
### テキスト
#### STOP・STOP-COFF入力停止方法が「減速停止」の場合の動作説明
(STOP-COFF入力がONの間にモーターが停止するとき)

1. 運転中にSTOP-COFF入力をONにすると、PLS-RDY出力がOFFになり、モーターが停止動作を開始します。モーターが停止すると、無励磁になります。  
2. STOP-COFF入力をOFFにすると、モーターが励磁して、PLS-RDY出力とREADY出力がONになります。

### 図（タイミングチャート）
```mermaid
flowchart TD
    subgraph チャート1
        direction LR
        A1[STOP-COFF入力 ON/OFF] --> B1[PLS-RDY出力 ON/OFF]
        B1 --> C1[READY出力 ON/OFF]
        C1 --> D1[MOVE出力 ON/OFF]
        D1 --> E1[モーター動作]
        E1 --> F1[モーター励磁（励磁/無励磁）]
        F1 --> G1[電磁ブレーキ（保持/解放）]
    end

    subgraph チャート2
        direction LR
        A2[STOP-COFF入力 ON/OFF] --> B2[PLS-RDY出力 ON/OFF]
        B2 --> C2[READY出力 ON/OFF]
        C2 --> D2[MOVE出力 ON/OFF]
        D2 --> E2[モーター動作]
        E2 --> F2[モーター励磁（励磁/無励磁）]
        F2 --> G2[電磁ブレーキ（保持/解放）]
    end
```

**図の説明:**  
- 上段チャートはSTOP-COFF入力のON/OFFに伴う各信号の変化とモーター動作の流れを示す。  
- 下段チャートは時間軸に沿った各信号のON/OFFの継続時間や遅延時間（例：2ms以上、250ms以下など）を示している。  
- モーター動作はSTOP-COFF入力ON時に減速し停止し、OFF時に励磁が再開される。  
- 電磁ブレーキは停止時に保持、動作時に解放される。  

### 注記
- ※駆動条件によって動作時間は異なる。
</contents>