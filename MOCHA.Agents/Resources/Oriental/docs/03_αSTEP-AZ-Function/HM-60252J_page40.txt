ファイル名: HM-60252J_page40

<overview>
本ページは「ラウンド設定」に関する説明で構成されています。  
テキスト部分ではラウンド機能の概要、使用しない場合の設定手順、使用する場合の設定例が記載されています。  
表部分はパラメータ設定画面の一部が画像で示されています。  
図部分はラウンド機能を使用する場合のパラメータ設定手順を示すフローチャートが含まれています。
</overview>

<contents>
### テキスト
#### 7 ラウンド設定
ラウンド機能とは、モーター出力軸の回転数が設定した範囲を超えると、現在位置の位置情報が自動的にプリセットされる機能です。  
ラウンドオフセットを設定すると、装置の動作領域を制限したり、インデックステーブルをプラス側とマイナス側の座標で制御することも可能です。

##### ■ラウンド機能を使用しない場合
- 「ラウンド（RND）設定」パラメータを無効にします。（初期値：有効）  
1. ツリービューから「パラメータ」→「モーター・機構（座標/JOG/原点復帰）設定」をクリックし、モーター・機構パラメータを表示。  
2. 「初期座標生成・ラウンド座標設定」パラメータを「マニュアル設定」にする。  
3. 「ラウンド（RND）設定」パラメータを「無効」にする。

##### ■ラウンド機能を使用する場合
- 設定例：モーター出力軸が18回転したときに、インデックステーブルを1回転させる。  
以下の手順でパラメータを設定する。

### 図
```mermaid
flowchart TD
    A[ラウンド機能の設定] --> B[ラウンド範囲の設定]
    B --> C[ラウンド範囲のオフセット比率を設定]
    C --> D[ラウンド範囲のオフセット量を設定]
    D --> E[設定したパラメータをドライバに書き込み]
```

### 表（画像内のパラメータ設定例）
| No. | パラメータ名                         | 設定値       | 備考               |
| :-- | :---------------------------------- | :----------- | :----------------- |
| 10  | 初期座標生成・ラウンド座標設定      | マニュアル設定 |                    |
| 12  | 初期座標生成・ラウンドオフセット比率 | 50.00        |                    |
| 13  | 初期座標生成・ラウンドオフセット補正値[step] | 0            |                    |
| 14  | ラウンド（RND）設定                 | 有効/無効    | 使用しない場合は無効 |

</contents>