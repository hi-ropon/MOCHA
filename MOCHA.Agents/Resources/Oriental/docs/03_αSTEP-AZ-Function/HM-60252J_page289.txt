ファイル名: HM-60252J_page289

<overview>
このページは「間接参照」について説明しています。構成は以下の通りです。
- テキスト部分：間接参照の概要と設定方法の説明
- 図部分：間接参照アドレスとエリアの関係を示す図解
- 表部分：間接参照のアドレスとエリアの名称および内容をまとめた表
</overview>

<contents>
### テキスト
#### 間接参照
間接参照は、送信専用のアドレス（間接参照アドレス）にデータを格納して設定する方法です。設定したいデータのアドレスが連続していなくても、間接参照アドレスが連続しているため、1つのクエリで送信できます。設定したいデータのアドレスは間接参照の「アドレス」に格納し、データの設定値は間接参照の「エリア」に格納します。

#### 間接参照のアドレスとエリア
間接参照のアドレスとエリアは、それぞれ32個（0～31）あります。

### 図
```mermaid
flowchart LR
    subgraph 運転データ
        A1[No.0 位置 10000]
        A2[No.1 位置 1500]
        A3[No.2 位置 20000]
        A4[No.3 位置 8000]
        B1[No.0 速度 3000]
        B2[No.1 速度 10000]
        B3[No.2 速度 25000]
        B4[No.3 速度 4500]
        C1[No.0 停止レート 1000.000]
        C2[No.1 停止レート 500.000]
        C3[No.2 停止レート 770.000]
        C4[No.3 停止レート 90.000]
    end

    subgraph 間接参照アドレス
        D1[間接参照アドレス0 上位:4864(1300h) 下位:4865(1301h)]
        D2[間接参照アドレス1 上位:4866(1302h) 下位:4867(1303h)]
        D3[間接参照アドレス2 上位:4868(1304h) 下位:4869(1305h)]
    end

    subgraph 間接参照エリア
        E1[間接参照エリア0 上位:4928(1340h) 下位:4929(1341h)]
        E2[間接参照エリア1 上位:4930(1342h) 下位:4931(1343h)]
        E3[間接参照エリア2 上位:4932(1344h) 下位:4933(1345h)]
    end

    A2 -->|No.1 位置のアドレス| D1
    A4 -->|No.3 速度のアドレス| D3
    C2 -->|No.1 停止のアドレス| D2

    D1 -->|No.1 位置のデータ| E1
    D2 -->|No.2 停止のデータ| E2
    D3 -->|No.3 速度のデータ| E3

    note right of D1
        アドレスが連続しているため、
        1回のクエリ送信で済む
    end
```

### 表
#### 間接参照のアドレスとエリア一覧

| 名称                 | 内容                                                                                         |
|----------------------|----------------------------------------------------------------------------------------------|
| 間接参照(0)対象アドレス | 間接参照で送信するデータのIDを格納します。IDはドライバが内部で保持する固有番号で、各設定項目に割り付けられています。Modbus通信ではIDの2倍の値がレジスタアドレスになるため、必ず「レジスタアドレスの半分の値」を入力してください。 |
| 間接参照(1)対象アドレス | 同上                                                                                         |
| ...                  | ...                                                                                          |
| 間接参照(30)対象アドレス| 同上                                                                                         |
| 間接参照(31)対象アドレス| 同上                                                                                         |
| 間接参照エリア0         | 間接参照で送信するデータの設定値を格納します。                                               |
| 間接参照エリア1         | 同上                                                                                         |
| ...                  | ...                                                                                          |
| 間接参照エリア30        | 同上                                                                                         |
| 間接参照エリア31        | 同上                                                                                         |
</contents>