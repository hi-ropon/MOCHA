ファイル名: HM-60252J_page89

<overview>
このページは「自動順送運転」について説明しています。構成は以下の通りです。
- テキスト部分：自動順送運転の概要、使用例、タイミングチャートの説明
- 表部分：複数の運転データのパラメータ一覧（名前、方式、位置、速度、起動・変速レート、停止レート、運転電流、運転終了遅延、結合、結合先）
- グラフ部分：運転イメージ（速度と位置の関係）、運転イメージ（速度と時間の関係）、タイミングチャート（START入力、M0～M7入力、READY出力、SEQ-BSY出力、内部速度指令の状態変化）
</overview>

<contents>
### テキスト
#### 自動順送運転
2つ以上の運転を自動で順番に実行します。1つの運転が終了した後、「運転終了遅延」に設定した時間だけ停止してから、「結合先」で設定した運転データの運転を開始します。途中で「結合無」を設定した運転データがあると、その運転データまでストアードデータ運転を行わず、モーターを停止させます。

#### 使用例
自動で複数の座標に位置決め運転を行なう場合の例が示されています。

#### タイミングチャートの説明
1. READY出力がONであることを確認します。
2. M0～M7入力で運転データNo.を選択します。
3. START入力をONにします。READY出力がOFF、SEQ-BSY出力がONになり、モーターが運転を開始します。
4. READY出力がOFFになっていることを確認し、START入力をOFFにします。
5. 最初の運転が終了すると、運転終了遅延で設定した時間だけ停止してから、自動順送で結合された運転が始まります。
6. 結合されたすべての運転が終わると、SEQ-BSY出力がOFF、READY出力がONになります。

### 表
#### 運転データ例（自動で複数の座標に位置決め運転を行なう場合）

| 名前  | 方式       | 位置 [step] | 速度 [Hz] | 起動・変速レート [kHz/s] | 停止レート [kHz/s] | 運転電流 [%] | 運転終了遅延 [s] | 結合   | 結合先[No.] |
| :---- | :--------- | :---------- | :-------- | :----------------------- | :----------------- | :----------- | :--------------- | :----- | :---------- |
| No.0  | 絶対位置決め | 1000        | 1500      | 15.000                   | 15.000             | 100.0        | 5.000            | 自動順送 | ↓(+1)       |
| No.1  | 絶対位置決め | 2000        | 2000      | 20.000                   | 20.000             | 100.0        | 5.000            | 自動順送 | ↓(+1)       |
| No.2  | 絶対位置決め | 300         | 1500      | 10.000                   | 10.000             | 100.0        | 0.000            | 結合無  | Stop        |

### 図
#### 運転イメージ（位置と速度の関係）
```mermaid
flowchart LR
    subgraph 位置軸
        A1[0] --> A2[300] --> A3[1000] --> A4[2000]
    end
    subgraph 速度軸
        B1[0] --> B2[1500] --> B3[2000]
    end
    A2 -->|No.2| B1
    A3 -->|No.0| B2
    A4 -->|No.1| B3
```

#### 運転イメージ（時間と速度の関係）
```mermaid
flowchart LR
    START --> T1[0s] --> T2[5s] --> T3[10s] --> T4[15s] --> T5[20s] --> T6[70s]
    T1 -->|No.0 速度15| V1[1500Hz]
    T2 -->|No.1 速度20| V2[2000Hz]
    T5 -->|No.2 速度10| V3[-1500Hz]
```

#### タイミングチャート
```mermaid
sequenceDiagram
    participant START入力
    participant M0～M7入力
    participant READY出力
    participant SEQ-BSY出力
    participant 内部速度指令

    Note over START入力,M0～M7入力: ① START入力ON、M0～M7入力選択
    START入力->>READY出力: ② READY出力OFF
    READY出力->>SEQ-BSY出力: ③ SEQ-BSY出力ON
    SEQ-BSY出力->>内部速度指令: ⑤ モーター運転開始
    READY出力->>START入力: ④ START入力OFF確認
    内部速度指令->>SEQ-BSY出力: ⑥ 運転終了後SEQ-BSY出力OFF、READY出力ON
```
</contents>