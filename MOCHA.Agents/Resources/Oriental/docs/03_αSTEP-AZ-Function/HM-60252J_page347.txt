ファイル名: HM-60252J_page347

<overview>
本ページは「簡易ダイレクトデータ運転モニタ0の使い方」について説明しています。構成は以下の通りです。
- テキスト部分：運転データNo.1の設定例、運転とモニタの手順（概要説明）
- 表部分：運転データNo.1の設定例の設定項目を表形式で示す
- グラフ部分：速度と時間の関係を示すグラフ（運転データの動作イメージ）
</overview>

<contents>
### テキスト
#### 簡易ダイレクトデータ運転モニタ0の使い方
例として、運転データNo.1の「位置」に「8,500」を書き込みます。

##### ■運転データNo.1の設定例
簡易ダイレクトデータ運転では、表に示した設定項目を使用します。運転終了遅延や結合など、表に示していない項目は設定しても無効です。

##### ■運転とモニタの手順
●概要  
1. リモートI/Oで、運転データNo.1を選択します。  
　M0だけをONにすると、データNo.1が選択されます。  
2. データを書き込むために、書き込み要求（D-REQまたはTRIG）をONにします。  
　・コマンド：「簡易ダイレクトデータ運転モニタ0（NETC）」コマンド  
　・データ：8,500 step  
　データを書き込むと同時に、データNo.1の運転が起動します。  
　書き込み要求がONの間、「簡易ダイレクトデータ運転モニタ0選択」パラメータで設定した項目をモニタし続けます。  
　モーターは8,500 stepの位置まで動作して止まります。  
3. 簡易ダイレクトデータ運転を終了します。  
　書き込み要求（D-REQまたはTRIG）をOFFにすると、レスポンスの更新が停止します。書き込み要求をOFFにしても、運転には影響ありません。

**memo**  
簡易ダイレクトデータ運転は、運転中でも「位置」データを更新できます。その場合は書き込み要求をOFFにしてから、別の目標位置の書き込み要求をONにしてください。

### 表
#### 運転データNo.1の設定例

| 方式           | 位置           | 速度       | 起動・変速レート | 停止レート   | 運転電流   |
| :------------- | :------------- | :--------- | :--------------- | :----------- | :--------- |
| 絶対位置決め   | 0 step (初期値) | 2,000 Hz  | 1.5 kHz/s        | 1.5 kHz/s    | 100.0 %    |

### 図
#### 速度と時間の関係グラフ
```mermaid
flowchart LR
    A[開始] --> B[速度 0 → 2,000 Hz (1.5 kHz/s)]
    B --> C[速度 2,000 Hz 維持 (3秒間)]
    C --> D[速度 2,000 Hz → 0 (1.5 kHz/s)]
    D --> E[停止]

    subgraph 時間軸
    direction LR
    T1[1秒] --> T2[3秒] --> T3[1秒]
    end

    B --- T1
    C --- T2
    D --- T3
```

**図の説明:**  
- 最初の1秒間で速度が0から2,000 Hzまで加速（1.5 kHz/s）  
- 次の3秒間は速度2,000 Hzを維持  
- 最後の1秒間で速度が2,000 Hzから0まで減速（1.5 kHz/s）  
- 合計5秒間の動作で8,500 stepの位置まで移動するイメージを示す
</contents>