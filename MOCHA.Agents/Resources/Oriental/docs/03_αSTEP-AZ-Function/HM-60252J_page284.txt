ファイル名: HM-60252J_page284

<overview>
本ページは「8-3 連続運転」に関する説明で構成されています。  
テキスト部分では、連続運転の設定例と運転手順の説明があり、  
表部分では「クエリ」と「レスポンス」の通信データ構造が詳細に示されています。  
グラフや図はなく、主にテキストと表で構成された技術的な通信設定の解説ページです。
</overview>

<contents>
### テキスト
#### 8-3 連続運転
連続運転を実行する方法の説明です。

##### 設定例
- 号機番号（サーバーアドレス）：1
- 運転データNo.：0
- 回転方向：FWD方向（正転）
- 運転速度：5,000 Hz

##### 運転手順
1. 次のクエリを送信して、運転データNo.0の運転速度を5,000 Hzに設定します。

2. 次のクエリを送信して、FW-POSをONにします。連続運転が開始されます。

※ FW-POSは初期設定でドライバ入力指令(007Dh)のbit15に割り付けられています。  
(2進数で0100 0000 0000 0000=16進数で4000h)

---

### 表

#### クエリ（運転速度設定）

| フィールド名称               | データ | 内容                                   |
|-----------------------------|--------|--------------------------------------|
| サーバーアドレス            | 01h    | サーバーアドレス1                     |
| ファンクションコード        | 10h    | 複数の保持レジスタへの書き込み       |
| レジスタアドレス（上位）    | 04h    | 書き込みの起点となるレジスタアドレス =運転速度No.0(0480h) |
| レジスタアドレス（下位）    | 80h    |                                      |
| レジスタ数（上位）          | 00h    | 起点のレジスタアドレスから書き込む   |
| レジスタ数（下位）          | 02h    | レジスタの数=2個(0002h)              |
| バイト数                    | 04h    | クエリのレジスタ数の2倍の値=4        |
| レジスタアドレスのライト値（上位） | 00h    | レジスタアドレス0480hの書き込み値   |
| レジスタアドレスのライト値（下位） | 00h    |                                      |
| レジスタアドレス+1のライト値（上位） | 13h    | =運転速度5,000 Hz(0000 1388h)       |
| レジスタアドレス+1のライト値（下位） | 88h    |                                      |
| エラーチェック（下位）      | C4h    | CRC-16の計算結果                     |
| エラーチェック（上位）      | 59h    |                                      |

#### レスポンス（運転速度設定）

| フィールド名称               | データ | 内容               |
|-----------------------------|--------|--------------------|
| サーバーアドレス            | 01h    | クエリと同じ値     |
| ファンクションコード        | 10h    | クエリと同じ値     |
| レジスタアドレス（上位）    | 04h    | クエリと同じ値     |
| レジスタアドレス（下位）    | 80h    | クエリと同じ値     |
| レジスタ数（上位）          | 00h    | クエリと同じ値     |
| レジスタ数（下位）          | 02h    | クエリと同じ値     |
| エラーチェック（下位）      | 41h    | CRC-16の計算結果   |
| エラーチェック（上位）      | 10h    |                    |

---

#### クエリ（FW-POS ON）

| フィールド名称               | データ | 内容                                   |
|-----------------------------|--------|--------------------------------------|
| サーバーアドレス            | 01h    | サーバーアドレス1                     |
| ファンクションコード        | 06h    | 保持レジスタへの書き込み               |
| レジスタアドレス（上位）    | 00h    | 書き込みを行なうレジスタアドレス =ドライバ入力指令(007Dh) |
| レジスタアドレス（下位）    | 7Dh    |                                      |
| ライト値（上位）            | 40h    | レジスタアドレスに書き込む値 =FW-POS ON(4000h) |
| ライト値（下位）            | 00h    |                                      |
| エラーチェック（下位）      | 28h    | CRC-16の計算結果                     |
| エラーチェック（上位）      | 12h    |                                      |
</contents>