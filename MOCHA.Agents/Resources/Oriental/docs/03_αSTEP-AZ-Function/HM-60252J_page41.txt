ファイル名: HM-60252J_page41

<overview>
本ページは「ラウンド機能」の設定方法について説明しています。構成は以下の通りです。
- テキスト部分：ラウンド機能の設定手順（STEP 1、STEP 2）を詳細に解説。
- 表部分：関連するパラメータの一覧表（パラメータ名、内容、初期値）を掲載。
- 画像部分：ツリービューの画面イメージやパラメータ設定画面のスクリーンショットが挿入されている。
グラフは含まれていません。
</overview>

<contents>
### テキスト
#### STEP 1 ラウンド機能を設定します
「ラウンド（RND）設定」パラメータを設定する手順です。

1. ツリービューから「パラメータ」→「モーター・機構（座標/JOG/原点復帰）設定」をクリックし、モーター・機構パラメータを表示させる。
2. 「ラウンド（RND）設定」パラメータを「有効」に設定する。

#### STEP 2 ラウンド範囲を設定します
「初期座標生成・ラウンド設定範囲」パラメータを設定します。設定した回数だけモーターが回転すると、現在位置の位置情報が自動的にプリセットされます。回転機構を使って一方向運転や近回り運転を行う際に設定します。

1. ツリービューから「パラメータ」→「モーター・機構（座標/JOG/原点復帰）設定」をクリックし、モーター・機構パラメータを表示させる。
2. 「初期座標生成・ラウンド設定範囲」パラメータを設定する。  
   例：ABZOセンサの内部座標は900 revまたは1,800 rev。ここではモーター出力軸が18回転したら位置情報がプリセットされるように「18」を設定。

### 表
#### 関連するパラメータ

| MEXE02ツリー表示 | パラメータ名                         | 内容                                                                                         | 初期値 |
| :---------------- | :---------------------------------- | :------------------------------------------------------------------------------------------- | :----- |
| モーター・機構    | ラウンド（RND）設定                 | ラウンド機能を設定します。<br>【設定範囲】0：無効 1：有効                                    | 1      |
|                   | 初期座標生成・ラウンド設定範囲     | ラウンド範囲を設定します。ここで設定した回数だけモーターが回転すると、現在位置の位置情報が自動的にプリセットされます。<br>【設定範囲】42ページ参照（1=0.1 rev） | 10     |
|                   | 初期座標生成・ラウンドオフセット比率設定 | ラウンド範囲のオフセット比率を設定します。<br>【設定範囲】0～10,000（1=0.01%）               | 5,000  |
|                   | 初期座標生成・ラウンドオフセット値設定   | ラウンド範囲のオフセット量を設定します。<br>【設定範囲】-536,870,912～536,870,911 step       | 0      |

### 画像説明
- ツリービュー画面：パラメータツリーの「モーター・機構（座標/JOG/原点復帰）設定」部分を示す。
- パラメータ設定画面：ラウンド（RND）設定の有効化や初期座標生成・ラウンド設定範囲の数値入力欄を示す。

</contents>