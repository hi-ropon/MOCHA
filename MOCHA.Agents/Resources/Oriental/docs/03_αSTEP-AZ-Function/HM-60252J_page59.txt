ファイル名: HM-60252J_page59

<overview>
このページは「目標位置の設定方法」について説明しています。構成は以下の通りです。
- テキスト部分：目標位置の設定方法の種類と概要説明
- 図部分：3種類の位置決め方法（絶対位置決め、相対位置決め、ラウンド絶対位置決め）の動作イメージ図
- 表はなし
</overview>

<contents>
### テキスト
#### 目標位置の設定方法
目標位置の設定方法には、以下の3種類があります。

- 絶対位置決め（アブソリュート）  
  原点を基準とした座標上の目標位置を設定します。  
  例：現在位置「100」から目標位置「400」へ移動する場合の設定

- 相対位置決め（インクリメンタル）  
  移動した先を次の移動の開始点として、目標位置を設定します。同じ移動量を繰り返すような運転に適しています。  
  例：現在位置「100」から目標位置「400」へ移動する場合の設定

- ラウンド絶対位置決め（ラウンドアブソリュート）  
  「ラウンド（RND）設定」パラメータを「有効」に設定して使用します。ラウンド範囲内の目標位置を設定します。  
  例：現在位置「100」から目標位置「400」へ移動する場合の設定

### 図
#### 絶対位置決め（アブソリュート）
```mermaid
flowchart LR
    原点(0) --> 現在位置(100) --> 目標位置(400)
    現在位置 -->|実際の移動量=300| 目標位置
    現在位置 -->|設定=400| 目標位置
```

#### 相対位置決め（インクリメンタル）
```mermaid
flowchart LR
    原点(0) --> 現在位置(100) --> 目標位置(400)
    現在位置 -->|実際の移動量=300| 目標位置
    現在位置 -->|設定=300| 目標位置
```

#### ラウンド絶対位置決め（ラウンドアブソリュート）
```mermaid
graph TD
    円グラフ
    原点(0) --> 現在位置(100)
    現在位置 --> 目標位置(400)
    目標位置 -->|設定=400| 範囲内
    目標位置 -->|実際の移動量=300| 範囲内
```
（円グラフは原点を中心に、現在位置100、目標位置400が示され、設定値400、実際の移動量300が記載されている）

</contents>