ファイル名: HM-60252J_page279

<overview>
このページはRS-485通信（Modbus）に関するパラメータ設定例と設定方法について説明しています。構成は以下の通りです。
- テキスト部分：通信順序の説明、パラメータの設定例、パラメータを強制的に初期値に戻す方法の説明
- 表部分：通信順序のパラメータ設定例、書き換え可能なパラメータ一覧、初期値に戻す際のパラメータ一覧
- グラフ・図はなし
</overview>

<contents>
### テキスト
#### 通信順序（Modbus）パラメータの設定例
32bitのデータ「1234 5678h」がレジスタアドレス1000hと1001hに格納される場合、パラメータ設定により配置が変わることを説明しています。

#### 7-2 書き換えるとすぐに反映されるパラメータ
MEXE02またはRS-485通信で設定可能なパラメータを示しています。

#### 7-3 パラメータを強制的に初期値へ戻す（デフォルト機能）
RS-485通信に関するパラメータを強制的に初期値に戻す手順を説明しています。
1. SW1スイッチのNo.2をONにし、Modbusプロトコルを選択
2. BAUDスイッチを「7」に設定
これにより、以下のパラメータが初期値に戻ります。

### 表
#### 通信順序（Modbus）パラメータの設定例

| パラメータの設定 | 1000h（偶数アドレス）上位 | 1000h（偶数アドレス）下位 | 1001h（奇数アドレス）上位 | 1001h（奇数アドレス）下位 |
| :--- | :---: | :---: | :---: | :---: |
| 0: Even Address-High Word & Big-Endian | 12h | 34h | 56h | 78h |
| 1: Even Address-Low Word & Big-Endian | 56h | 78h | 12h | 34h |
| 2: Even Address-High Word & Little-Endian | 34h | 12h | 78h | 56h |
| 3: Even Address-Low Word & Little-Endian | 78h | 56h | 34h | 12h |

#### 7-2 書き換えるとすぐに反映されるパラメータ一覧

| MEXE02ツリー表示 | パラメータ名 | 内容 | 初期値 |
| :--- | :--- | :--- | :---: |
| 通信・I/F機能 | 通信タイムアウト（Modbus） | RS-485通信の通信タイムアウト発生条件設定。設定範囲：0（監視しない）～10,000ms | 0 |
|  | 通信異常アラーム（Modbus） | RS-485通信異常発生時にアラーム発生。設定範囲：1～10回 | 3 |
|  | サーバーエラー検出時応答（Modbus） | 0:サーバーエラー発生時も正常応答返す<br>1:サーバーエラー発生時は例外応答返す | 1 |
|  | テストモードタイムアウト（Modbus） | 予約機能。使用不可 | 300 |

#### 7-3 パラメータを強制的に初期値へ戻す際のパラメータ一覧

| MEXE02ツリー表示 | パラメータ名 | 初期値 |
| :--- | :--- | :---: |
| 通信・I/F機能 | Baudrate（Modbus） | -1：ドライバのスイッチ設定に従う |
|  | 通信順序（Modbus） | 0：Even Address-High Word & Big-Endian |
|  | 通信パリティ（Modbus） | 1：偶数パリティ |
|  | 通信ストップビット（Modbus） | 0：1ビット |
|  | 送信待ち時間（Modbus） | 30（3ms） |
|  | サイレントインターバル（Modbus） | 0：自動 |

</contents>