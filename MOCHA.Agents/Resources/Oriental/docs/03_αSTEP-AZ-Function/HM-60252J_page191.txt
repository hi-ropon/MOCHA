ファイル名: HM-60252J_page191

<overview>
このページは、FW-BLK入力およびRV-BLK入力に関する説明で構成されています。内容は主に以下の3つのセクションに分かれています。
1. テキスト部分：FW-BLK入力、RV-BLK入力の機能説明と動作条件、停止方法の詳細な説明。
2. 表部分：運転別の機能一覧表と関連パラメータの説明表。
3. グラフ部分：FW-BLK入力停止方法が「減速停止」の場合の信号のタイミングチャート。
</overview>

<contents>
### テキスト
#### FW-BLK入力、RV-BLK入力
- FW-BLK入力をONにするとFWD方向、RV-BLK入力をONにするとRVS方向の運転を停止する。
- それぞれの入力がONの間は、停止している方向の運転開始信号が入力されてもモーターは動かない。
- 反対方向の運転開始信号は機能する。

#### FW-BLK・RV-BLK入力停止方法が「減速停止」の場合（FW-BLK入力がONの間にモーターが停止するとき）
1. FWD方向の運転中にFW-BLK入力をONにすると、モーターが停止動作を開始。
2. 運転が停止するとREADY出力がONになる。
3. FW-BLK入力がONのときにRVS方向の運転開始信号を入力するとREADY出力がOFFになり、運転が開始。

### 表
#### 運転別の機能

| 運転の種類           | 機能                                                                                   |
|----------------------|----------------------------------------------------------------------------------------|
| パルス列運転         | モーターは即停止します。入力された信号に対応する方向のパルス入力が無効になります。     |
| ストアードデータ運転 | 「FW-BLK・RV-BLK入力停止方法」パラメータに従って、運転を停止します。残りの移動量はクリアされます。 |
| マクロ運転           | 上記と同様                                                                             |
| ダイレクトデータ運転 | 上記と同様                                                                             |

#### 関連するパラメータ

| MEXE02ツリー表示 | パラメータ名               | 内容                                                                                         | 初期値 |
|------------------|----------------------------|----------------------------------------------------------------------------------------------|--------|
| I/O動作・機能    | FW-BLK・RV-BLK入力停止方法 | FW-BLK入力またはRV-BLK入力がONになったときのモーターの停止方法を設定。<br>【設定範囲】0:即停止 1:減速停止 | 1      |

**memo**  
- FW-BLK入力、RV-BLK入力がONになると次のインフォメーションが発生。  
  ・FW-BLK入力がONの場合：「正転方向運転禁止状態」  
  ・RV-BLK入力がONの場合：「逆転方向運転禁止状態」

### 図（信号タイミングチャート）
```mermaid
flowchart TD
    subgraph 信号タイミング
        direction TB
        FWBLK_ON[FW-BLK入力 ON] -->|①| READY_ON[READY出力 ON]
        READY_ON -->|②| READY_OFF[READY出力 OFF]
        READY_OFF -->|③| モーター停止[モーター動作 減速停止]
    end

    FWBLK_ON -.-> PLSRDY[PLS-RDY出力 ON/OFF]
    FWBLK_ON -.-> MOVE[ MOVE出力 ON/OFF]

    モーター動作 -->|励磁| 励磁状態[モーター励磁]
    モーター動作 -->|無励磁| 無励磁状態[モーター無励磁]

    電磁ブレーキ[電磁ブレーキ] -->|保持| 保持状態
    電磁ブレーキ -->|解放| 解放状態
```

**図の説明:**  
- ① FW-BLK入力ONでモーター停止動作開始。  
- ② 運転停止後READY出力がONになる。  
- ③ FW-BLK入力ONのままRVS方向運転開始信号入力でREADY出力OFF、運転開始。  
- モーター励磁状態と電磁ブレーキの保持・解放状態も示されている。
</contents>