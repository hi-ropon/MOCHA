ファイル名: HM-60252J_page204

<overview>
本ページは「運転の管理」に関する説明で構成されています。主にテキストで各種出力信号の説明があり、関連パラメータを示す表が1つ、さらにIN-POS出力の動作を示すグラフが1つ掲載されています。テキスト部分は各出力信号の動作条件や意味を詳細に解説しており、表部分はMOVE出力に関するパラメータの設定内容を示しています。グラフ部分はIN-POS出力の判定距離と出力ON/OFFの関係を視覚的に表現しています。
</overview>

<contents>
### テキスト
#### 運転の管理

##### ■運転状態表示

- **READY出力**  
  ストアードデータ運転、マクロ運転、原点復帰運転の準備が完了するとREADY出力がONになる。  
  READY出力がONになってから運転開始指令をドライバに入力する。  
  READY出力がONになる条件は以下の通り：  
  ・ドライバの制御電源と主電源が投入されている  
  ・運転開始入力がすべてOFF  
  ・FREE入力がOFF  
  ・C-ON入力がON（C-ON入力が割り付けられている場合）  
  ・STOP入力がOFF  
  ・STOP-COFF入力がOFF  
  ・PAUSE入力がOFF  
  ・CLR入力がOFF  
  ・アラームが発生していない  
  ・モーターが運転していない  
  ・MEXE02でティーチング・リモート運転、ダウンロード、およびI/Oテストを実行していない  
  ・RS-485通信でConfigurationコマンド、データ一括初期化コマンド、全データ一括初期化コマンド、およびNVメモリー括読み出しコマンドを実行していない

- **MOVE出力**  
  モーターの動作中にMOVE出力がONになる。

##### 関連するパラメータ

| MEXE02ツリー表示 | パラメータ名           | 内容                                                                 | 初期値 |
|------------------|------------------------|----------------------------------------------------------------------|--------|
| I/O動作・機能     | MOVE出力最小ON時間     | MOVE出力の最小ON時間を設定する。<br>【設定範囲】0～255 ms            | 0      |

- **OPE-BSY出力**  
  ドライバが内部発振を行っているときにOPE-BSY出力がONになる。  
  内部発振が行われる運転は以下の通り：  
  ・ストアードデータ運転  
  ・マクロ運転  
  ・ダイレクトデータ運転  
  ・原点復帰運転

- **IN-POS出力**  
  位置決め運転の終了後、ローターが指令位置に対して「IN-POS出力判定距離」パラメータの値に収束したときにIN-POS出力がONになる。

### 図
```mermaid
flowchart LR
    A[目標位置] --> B[IN-POS出力判定距離]
    B --> C[IN-POS出力 ON/OFF]
    subgraph IN-POS出力の動作
        direction TB
        D[ローター位置の変動] --> E[判定距離内に収束]
        E --> C
    end
```

**図の説明:**  
目標位置に対してローターの位置がIN-POS出力判定距離内に収束するとIN-POS出力がONになる。位置が判定距離を外れるとOFFになる動作を示している。

</contents>