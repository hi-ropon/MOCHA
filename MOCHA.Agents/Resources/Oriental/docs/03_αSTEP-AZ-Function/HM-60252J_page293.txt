ファイル名: HM-60252J_page293

<overview>
このページは「設定例」として、間接参照を使って号機番号1にデータを送受信する方法を説明しています。ページ構成は以下の通りです。
- テキスト部分：設定例の説明、STEP1の概要説明
- 表部分：間接参照アドレスの登録に関する設定データ表とクエリの詳細表
- グラフ部分：なし
</overview>

<contents>
### テキスト
#### ■設定例
間接参照を使って、号機番号1にデータを送受信する例を説明します。

#### ●STEP1：間接参照アドレスへの登録
設定データとして、間接参照アドレスに送信するデータのIDを登録します。

---

### 表
#### 間接参照アドレス登録用設定データ

| 間接参照アドレス | レジスタアドレス上位 | レジスタアドレス下位 | 送信するデータ | ID |
| :--- | :---: | :---: | :--- | :--- |
| 間接参照(0)対象アドレス設定 | 1300h | 1301h | 運転データNo.1の位置 | C21h<br>（レジスタアドレス1842hの半分の値） |
| 間接参照(1)対象アドレス設定 | 1302h | 1303h | 運転データNo.2の停止レート | C44h<br>（レジスタアドレス1888hの半分の値） |
| 間接参照(2)対象アドレス設定 | 1304h | 1305h | 運転データNo.3の速度 | C62h<br>（レジスタアドレス18C4hの半分の値） |

---

#### クエリ詳細

| フィールド名称 | データ | 内容 |
| :--- | :---: | :--- |
| サーバーアドレス | 01h | サーバーアドレス1 |
| ファンクションコード | 10h | 複数の保持レジスタへの書き込み |
| レジスタアドレス(上位) | 13h | 書き込みの起点となるレジスタアドレス<br>=間接参照(0)対象アドレス設定(1300h) |
| レジスタアドレス(下位) | 00h | 同上 |
| レジスタ数(上位) | 00h | 起点のレジスタアドレスから書き込む |
| レジスタ数(下位) | 06h | レジスタの数=6個(0006h) |
| バイト数 | 0Ch | クエリのレジスタ数の2倍の値=12 |
| レジスタアドレスのライト値(上位) | 00h | レジスタアドレス1300hの書き込み値<br>=運転データNo.1位置のID(C21h) |
| レジスタアドレスのライト値(下位) | 00h | 同上 |
| レジスタアドレス+1のライト値(上位) | 0Ch | 同上 |
| レジスタアドレス+1のライト値(下位) | 21h | 同上 |
| レジスタアドレス+2のライト値(上位) | 00h | レジスタアドレス1302hの書き込み値<br>=運転データNo.2停止レートのID(C44h) |
| レジスタアドレス+2のライト値(下位) | 00h | 同上 |
| レジスタアドレス+3のライト値(上位) | 0Ch | 同上 |
| レジスタアドレス+3のライト値(下位) | 44h | 同上 |
| レジスタアドレス+4のライト値(上位) | 00h | レジスタアドレス1304hの書き込み値<br>=運転データNo.3速度のID(C62h) |
| レジスタアドレス+4のライト値(下位) | 00h | 同上 |
| レジスタアドレス+5のライト値(上位) | 0Ch | 同上 |
| レジスタアドレス+5のライト値(下位) | 62h | 同上 |
| エラーチェック(下位) | D7h | CRC-16の計算結果 |
| エラーチェック(上位) | A6h | 同上 |

---

### 図
なし
</contents>