ファイル名: HM-60252J_page354

<overview>
本ページは「パラメータの反映タイミング」についての説明で構成されています。  
テキスト部分では、パラメータの保存先や反映タイミングの概要、注意事項が記載されています。  
表部分では、反映タイミングの種類とその説明、READ/WRITEの表記についての説明が2つの表で示されています。  
グラフや図は含まれていません。
</overview>

<contents>
### テキスト
#### パラメータの反映タイミング
- ドライバで使用するデータはすべて32bit幅。Modbusプロトコルは16bit幅のレジスタ2個で1つのデータを表す。  
- パラメータはRAMまたはNVメモリに保存される。RAMのパラメータは制御電源を遮断すると消去されるが、NVメモリのパラメータは制御電源を遮断しても保存される。  
- 制御電源投入時にNVメモリのパラメータがRAMに転送され、RAM上で再計算やセットアップが行われる。  
- RS-485通信またはFAネットワークで設定したパラメータはRAMに保存される。NVメモリに保存するにはメンテナンスコマンドの「NVメモリ一括書き込み」を実行する必要がある。  
- MEXE02で設定したパラメータは「データの書き込み」を行うとNVメモリに保存される。  
- パラメータ変更時の反映タイミングはパラメータによって異なるため、「表記の規則」で確認すること。  

**memo**  
- RS-485通信またはFAネットワークで設定したパラメータはRAMに保存される。制御電源の再投入が必要なものは電源を切る前に必ずNVメモリへ保存すること。  
- NVメモリへの書き込み可能回数は約10万回。  

---

### 表
#### 反映タイミングについて
| 表記 | 内容               | 詳細                                                         |
| :--- | :----------------- | :----------------------------------------------------------- |
| A    | 即時反映           | パラメータを書き込むと、すぐに再計算とセットアップが行われる。 |
| B    | 運転停止後に反映   | 運転を停止すると、再計算とセットアップが行われる。           |
| C    | Configurationの実行後に反映 | Configurationの実行後または制御電源の再投入後に再計算とセットアップが行われる。 |
| D    | 制御電源の再投入後に反映 | 制御電源の再投入後に再計算とセットアップが行われる。           |

※ 制御電源を接続しないドライバは主電源になる。

---

#### READ、WRITEについて
| 表記 | 内容       |
| :--- | :--------- |
| R    | READ       |
| W    | WRITE      |
| R/W  | READ/WRITE |

</contents>