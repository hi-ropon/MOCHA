ファイル名: HM-60252J_page82

<overview>
このページは「ストアードデータ(SD)運転」に関する説明で構成されています。  
テキスト部分では「連続運転（押し当て）」の概要と運転方法の手順が記載されています。  
表部分には運転データの設定例が示されています。  
グラフ部分には速度と位置・時間の関係を示す運転イメージのグラフと、各入力・出力信号のタイミングチャートが掲載されています。
</overview>

<contents>
### テキスト
#### ストアードデータ(SD)運転
■ 連続運転（押し当て）  
運転データに運転速度を設定して運転を実行します。運転を実行すると起動速度で立ち上がり、運転速度になるまで加速します。運転速度に達すると速度を維持したまま運転を続けます。負荷に押し当たったときは、負荷に対して加圧し続けます。

● 使用例  
運転データの設定

#### 運転方法
1. READY出力がONであることを確認します。  
2. M0～M7入力で運転データNo.を選択し、START入力をONにします。READY出力がOFFになり、モーターが運転を開始します。  
3. READY出力がOFFになっていることを確認し、START入力をOFFにします。  
4. STOP入力をONにすると、モーターが減速停止を始めます。  
5. モーターが停止すると、READY出力がONになります。

### 表
#### 運転データの設定例

| 名前  | 方式               | 位置 [step] | 速度 [Hz] | 起動・変速レート [kHz/s] | 停止レート [kHz/s] | 運転電流 [%] |
| :---- | :----------------- | :---------- | :-------- | :----------------------- | :----------------- | :------------ |
| No.0  | 連続運転(押し当て) | 0           | 2000      | 1.500                    | 1.500              | 20.0          |

### 図
#### 運転イメージ（速度と位置・時間の関係）

```mermaid
flowchart LR
    subgraph 速度-位置グラフ
        A[速度 0] --> B[速度 500] --> C[速度 2000] --> D[押し当て 維持]
        A ---|位置| E[位置 0]
        D ---|位置| F[位置 増加]
    end

    subgraph 速度-時間グラフ
        G[速度 0] --> H[速度 500] --> I[速度 2000] --> J[押し当て 維持]
        H ---|時間| K[時間 増加]
        J ---|時間| L[時間 維持]
    end
```

#### 入出力信号タイミングチャート

```mermaid
flowchart TD
    READY[READY出力] -->|① ON| START[START入力]
    M0toM7[M0～M7入力] -->|② 運転データNo.選択| START
    START -->|③ ON| READY
    STOP[STOP入力] -->|④ ON| READY
    READY -->|⑤ ON| モーター停止
    モーター[内部速度指令] --> READY
```

**図の説明:**  
- READY出力がONであることを確認し、M0～M7入力で運転データNo.を選択後、START入力をONにする。  
- READY出力がOFFになりモーターが運転開始。  
- START入力をOFFにし、STOP入力をONにするとモーターが減速停止。  
- モーター停止後、READY出力がONに戻る。
</contents>