ファイル名: HM-60252J_page43

<overview>
このページは、ラウンド設定に関する操作手順を説明しています。テキスト部分では設定例の数値計算と条件説明、STEP 3としてラウンド範囲のオフセット比率設定手順を詳細に解説しています。表はなく、図はパラメータ設定画面のスクリーンショットと、ラウンド範囲の座標を示す簡単な座標図（数値軸と円座標）が含まれています。
</overview>

<contents>
### テキスト
#### 設定例2
- ABZセンサの内部座標：1,800 rev
- ラウンド設定範囲：14.4 rev
- 分解能：333.333・・・P/R（電子ギヤA=3、電子ギヤB=1）
- モーター：TSギヤードモーター（減速比3.6）

条件①  
\[
\frac{1,800}{14.4} = 125
\]

条件②  
\[
14.4 \times \frac{電子ギヤB}{電子ギヤA} \times 1,000 = 14.4 \times \frac{1}{3} \times 1,000 = 4,800
\]

条件①、②が両方とも整数なので、設定条件を満たしており、ラウンドが可能である。

---

#### STEP 3 ラウンド範囲のオフセット比率を設定します
「初期座標生成・ラウンドオフセット比率設定」パラメータを設定し、設定した比率だけラウンド範囲を負方向へオフセットできる。

1. ツリービューから「パラメータ」→「モーター・機構（座標/JOG/原点復帰）設定」をクリックし、モーター・機構パラメータを表示する。

2. 「初期座標生成・ラウンドオフセット比率設定」パラメータを設定する。  
ここでは、インデックステーブルがどちらの方向にも回転できるように、18回転分を＋と－に50%ずつ振り分ける。

---

### 図
- パラメータ設定画面のツリービューと該当パラメータのスクリーンショット

- 「ラウンド設定範囲」が18 rev、ラウンドオフセット比率が50%の場合の座標図

```mermaid
flowchart LR
    A[ラウンド設定範囲 18 rev] --> B[オフセット比率 50%]
    B --> C[座標軸 -9 -4.5 0 4.5 9]
    C --> D[円座標 -9 0 9]
```

---

### 重要事項
「ラウンド（RND）設定」パラメータや「初期座標生成・ラウンド設定範囲」パラメータを変更すると、絶対位置がずれる場合がある。パラメータ変更後は高速原点復帰運転または原点復帰運転を行うこと。

</contents>