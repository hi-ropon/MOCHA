ファイル名: HM-60252J_page241

<overview>
このページは「パラメータ：Direct-OUT機能選択（DOUT）」に関する説明で構成されています。  
主にテキストと表でパラメータ名、内容、設定範囲、初期値が整理されており、最後にOFF出力遅延時間に関する簡単な図（タイミングチャート）が掲載されています。  
テキスト部分は各パラメータの説明、表部分はパラメータの一覧と詳細、図部分はOFF出力遅延時間の動作イメージを示しています。
</overview>

<contents>
### テキスト
#### パラメータ：Direct-OUT機能選択（DOUT）
DOUT0～DOUT5までの各出力機能に割り付ける出力信号の選択や、接点設定（信号反転）、OFF出力遅延時間、コンポジット論理結合、コンポジット出力機能、コンポジット接点設定（信号反転）を設定します。

### 表
| パラメータ名                     | 内容                                                                 | 設定範囲                     | 初期値          |
| :------------------------------ | :------------------------------------------------------------------- | :--------------------------- | :-------------- |
| DOUT0（通常）出力機能           | DOUT0に割り付ける出力信号を選択します。                             | 出力信号一覧→252ページ       | 144:HOME-END    |
| DOUT1（通常）出力機能           | DOUT1に割り付ける出力信号を選択します。                             | 出力信号一覧→252ページ       | 138:IN-POS      |
| DOUT2（通常）出力機能           | DOUT2に割り付ける出力信号を選択します。                             | 出力信号一覧→252ページ       | 133:PLS-RDY     |
| DOUT3（通常）出力機能           | DOUT3に割り付ける出力信号を選択します。                             | 出力信号一覧→252ページ       | 132:READY       |
| DOUT4（通常）出力機能           | DOUT4に割り付ける出力信号を選択します。                             | 出力信号一覧→252ページ       | 134:MOVE        |
| DOUT5（通常）出力機能           | DOUT5に割り付ける出力信号を選択します。                             | 出力信号一覧→252ページ       | 130:ALM-B       |
| 接点設定（信号反転）            | DOUT0～DOUT5の接点設定を変更します。                               | 0:反転しない 1:反転する      | 0               |
| OFF出力遅延時間                 | DOUT0～DOUT5のOFF出力遅延時間を設定します。（図参照）               | 0～250 ms                   | 0               |
| コンポジット論理結合            | DOUT0～DOUT5のコンポジット論理結合を設定します。                   | 0:AND 1:OR                  | 1               |
| コンポジット出力機能            | DOUT0～DOUT5の信号と論理演算を行なう出力信号を選択します。         | 出力信号一覧→252ページ       | 128:CONST-OFF   |
| コンポジット接点設定（信号反転） | DOUT0～DOUT5のコンポジット出力機能の接点設定を変更します。         | 0:反転しない 1:反転する      | 0               |

### 図
OFF出力遅延時間のタイミングチャートを示しています。

```mermaid
flowchart LR
    subgraph 内部信号
        A1[ON] --> A2[OFF]
    end
    subgraph ダイレクト出力(DOUT)
        B1[ON] -->|OFF出力遅延時間| B2[OFF]
    end
    A1 --- B1
    A2 --- B2
```

**図の説明:**  
- 内部信号がONからOFFに変わった後、ダイレクト出力（DOUT）はOFF出力遅延時間分遅れてOFFになる動作を示しています。
</contents>