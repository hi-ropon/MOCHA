ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page110

<overview>
本ページはCPUユニット用命令の応用命令に関する説明で構成されています。主にテキストと表で構成されており、各命令の名称、処理内容、参照先ページが一覧形式で示されています。グラフや図はなく、数式や命令の処理内容を表形式で整理した構成です。
</overview>

<contents>
### テキスト
本ページは単精度および倍精度の実数に関する各種演算命令をまとめています。命令は平方根、指数演算、自然対数演算、べき乗演算などに分類されています。

### 表
#### 単精度実数平方根
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| ESQRT | 単精度実数で指定した値の平方根を演算します。 | 949ページ ESQRT(P) |
| ESQRTP | √(s)+1, (s) → (d)+1, (d) |

#### 倍精度実数平方根
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| EDSQRT | 倍精度実数で指定した値の平方根を演算します。 | 951ページ EDSQRT(P) |
| EDSQRTP | √(s)+3, (s)+2, (s)+1, (s) → (d)+3, (d)+2, (d)+1, (d) |

#### 単精度実数指数演算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| EXP | 単精度実数で指定した値の指数を演算します。 | 953ページ EXP(P) |
| EXPP |  |

#### 倍精度実数指数演算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| EXPD | 倍精度実数で指定した値の指数を演算します。 | 955ページ EXPD(P) |
| EXPDP |  |

#### 単精度実数自然対数演算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| LOG | 単精度実数で指定した値の自然対数(e)を底としたときの対数を演算します。 | 957ページ LOG(P) |
| LOGP |  |

#### 倍精度実数自然対数演算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| LOGD | 倍精度実数で指定した値の自然対数(e)を底としたときの対数を演算します。 | 959ページ LOGD(P) |
| LOGDP |  |

#### BCD4桁/8桁平方根
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| BSQRT | BCD値(4桁)で指定した値の平方根を演算します。 | 961ページ BSQRT(P) |
| BSQRTP | √(s) → (d), (d)+1<br>(d):整数部<br>(d)+1:小数部 |
| BDSQRT | BCD値(8桁)で指定した値の平方根を演算します。 | 963ページ BDSQRT(P) |
| BDSQRTP | √(s)+1, (s) → (d), (d)+1<br>(d):整数部<br>(d)+1:小数部 |

#### 単精度実数べき乗演算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| POW | 単精度実数のべき乗を演算します。 | 965ページ POW(P) |
| POWP |  |

#### 倍精度実数べき乗演算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| POWD | 倍精度実数のべき乗を演算します。 | 967ページ POWD(P) |
| POWDP |  |
</contents>