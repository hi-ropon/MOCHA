ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page71

<overview>
本ページは命令コードに関する説明で構成されており、主にテキストと表で構成されています。テキスト部分では各命令の概要が示され、表部分では命令記号、処理内容、参照先ページが整理されています。グラフや図は含まれていません。
</overview>

<contents>
### テキスト
本ページは「BIN32ビット乗除算」「BCD4桁加減算」「BCD8桁加減算」「BCD4桁乗除算」「BCD8桁乗除算」という5つの命令群について説明しています。

### 表
#### BIN32ビット乗除算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| D* / D*P / D*_U / D*P_U | 指定した2つのBIN32ビットデータを乗算します。 | 252ページ D*(P)(_U) |
| D/ / D/P / D/_U / D/P_U | 指定した2つのBIN32ビットデータを除算します。 | 254ページ D/(P)(_U) |

#### BCD4桁加減算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| B+ / B+P | 指定した2つのBCD4桁データを加算します。（オペランドが2個の場合） | 256ページ B+(P) [オペランドが2個の場合] |
| B+ / B+P | 指定した2つのBCD4桁データを加算します。（オペランドが3個の場合） | 257ページ B+(P) [オペランドが3個の場合] |
| B- / B-P | 指定した2つのBCD4桁データを減算します。（オペランドが2個の場合） | 259ページ B-(P) [オペランドが2個の場合] |
| B- / B-P | 指定した2つのBCD4桁データを減算します。（オペランドが3個の場合） | 260ページ B-(P) [オペランドが3個の場合] |

#### BCD8桁加減算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| DB+ / DB+P | 指定した2つのBCD8桁データを加算します。（オペランドが2個の場合） | 262ページ DB+(P) [オペランドが2個の場合] |
| DB+ / DB+P | 指定した2つのBCD8桁データを加算します。（オペランドが3個の場合） | 264ページ DB+(P) [オペランドが3個の場合] |
| DB- / DB-P | 指定した2つのBCD8桁データを減算します。（オペランドが2個の場合） | 266ページ DB-(P) [オペランドが2個の場合] |
| DB- / DB-P | 指定した2つのBCD8桁データを減算します。（オペランドが3個の場合） | 268ページ DB-(P) [オペランドが3個の場合] |

#### BCD4桁乗除算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| B* / B*P | 指定した2つのBCD4桁データを乗算します。 | 270ページ B*(P) |
| B/ / B/P | 指定した2つのBCD4桁データを除算します。 | 272ページ B/(P) |

#### BCD8桁乗除算
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| DB* / DB*P | 指定した2つのBCD8桁データを乗算します。 | 274ページ DB*(P) |
| DB/ / DB/P | 指定した2つのBCD8桁データを除算します。 | 276ページ DB/(P) |
</contents>