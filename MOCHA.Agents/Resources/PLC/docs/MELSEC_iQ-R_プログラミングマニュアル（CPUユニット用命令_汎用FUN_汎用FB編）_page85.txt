ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page85

<overview>
本ページは命令コードごとの処理内容を説明した表形式の構成です。ページ全体は以下のセクションで構成されています。
・表部分：命令記号、処理内容、参照先の3列からなる表が2つあり、上段はダブルワードのシフト命令、下段は単精度実数のn点の右シフト・左シフト命令をまとめています。
・図部分：各命令の処理内容を示すデータのシフトイメージ図が表内に複数挿入されています。
テキスト部分は表の説明文として命令の動作説明が記載されています。
</overview>

<contents>
### 表
#### ダブルワードのシフト命令
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| SFTDWL / SFTDWLP | 指定したデバイスから(n1)ダブルワード分のデータ範囲内で、左へ(n2)ダブルワード分シフトさせます。シフト後の空きデータには0が格納されます。 | 390ページ SFTDWLP |
| DWSFTL / DWSFTLP | 指定したデバイスから(n1)ダブルワード分のデータ範囲内で、左へ(n2)ダブルワード分シフトさせます。シフト後の空きデータには指定データが格納されます。 | 392ページ DWSFTL(P) |

図は(n1)の範囲内で(n2)分シフトし、空いた部分に0または指定データを格納する様子を示しています。

#### 単精度実数n点のn点右シフト、左シフト命令
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| SFTER / SFTERP | 指定したデバイスから単精度実数(n1)点分のデータ範囲内で、右へ(n2)点分シフトさせます。シフト後の空きデータには0が格納されます。 | 394ページ SFTER(P) |
| ESFTR / ESFTRP | 指定したデバイスから単精度実数(n1)点分のデータ範囲内で、右へ(n2)点分シフトさせます。シフト後の空きデータには指定データが格納されます。 | 396ページ ESFTR(P) |
| SFTEL / SFTELP | 指定したデバイスから単精度実数(n1)点分のデータ範囲内で、左へ(n2)点分シフトさせます。シフト後の空きデータには0が格納されます。 | 398ページ SFTEL(P) |
| ESFTL / ESFTLP | 指定したデバイスから単精度実数(n1)点分のデータ範囲内で、左へ(n2)点分シフトさせます。シフト後の空きデータには指定データが格納されます。 | 400ページ ESFTL(P) |

図は単精度実数のデータ配列を(n1)点分の範囲で(n2)点分右または左にシフトし、空いた部分に0または指定データを格納する様子を示しています。

### テキスト
- 「指定したデバイスから(n1)ダブルワード分のデータ範囲内で、左へ(n2)ダブルワード分シフトさせます。シフト後の空きデータには0または指定データが格納されます。」といった命令の動作説明が記載されています。
- 単精度実数のシフト命令も同様に、右シフト・左シフトの動作と空きデータの扱いについて説明されています。

</contents>