ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page37

<overview>
このページは「ワード型ラベルにおけるビットデータの扱い」について説明しています。構成は以下の通りです。
- テキスト部分：ワード型ラベルでビットNo.指定を行うことで特定のビットデータを扱う方法の説明と例示。
- 図部分：ビット指定の表記方法を示す3つの例（L_INT.0、L_INT[1].0、L_STRUCT[1].S_INT.0）を矢印で分解して解説。
- 表部分：ビット指定が可能なラベルのデータ型一覧を表形式で提示。
グラフ部分はありません。
</overview>

<contents>
### テキスト
#### ワード型ラベルにおけるビットデータの扱い
ワード型ラベルでは、ビットNo.指定を行うことで、指定したビットNo.のビットデータを扱うことができます。ビット指定の表記方法は「ラベル名.ビットNo.」となります。

##### 例
- L_INT.0
- L_INT[1].0
- L_STRUCT[1].S_INT.0

これらの例はそれぞれ「ラベル名」「ビット指定」「ビットNo.」の構成要素に分解されます。

### 図
- **L_INT.0**  
  「L_INT」がラベル名、「0」がビットNo.を示す。
- **L_INT[1].0**  
  「L_INT」がラベル名、「[1]」がインデックス指定、「0」がビットNo.。
- **L_STRUCT[1].S_INT.0**  
  「L_STRUCT[1]」が構造体ラベル名、「S_INT」がラベル名、「0」がビットNo.。

### 表
#### ビット指定が可能なラベルのデータ型

| 項目                         | データ型                                                                                   |
|------------------------------|--------------------------------------------------------------------------------------------|
| ビット指定が可能なラベルのデータ型 | ・ワード（符号付き）(INT型) <br> ・ワード（符号なし）ビット列[16ビット](WORD型) <br> ・タイマ（TIMER型）の現在値(N)*1 <br> ・積算タイマ（RETENTIVETIMER型）の現在値(N)*1 <br> ・カウンタ（COUNTER型）の現在値(N)*1 |

*1 ラダーの場合は指定できません。
</contents>