ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page1474

<overview>
本ページは主にテキストと表で構成されています。テキスト部分では、演算結果の説明とエラーに関する注意事項が記載されています。表部分は2つあり、1つは演算条件と演算結果の対応を示す表、もう1つはエラーコードとその内容を示す表です。グラフや図は含まれていません。
</overview>

<contents>
### テキスト
#### 演算結果
1. EN/ENOなしファンクション  
演算処理を実行し、(d)から演算出力値が出力されます。

2. EN/ENO付きファンクション  
実行条件と演算結果は以下の通りです。

#### 注意事項  
* ENOからFALSEが出力された場合、(d)から出力されるデータは不定値となります。  
* その場合は、(d)から出力されたデータを使用しないようプログラムの処理を行ってください。

### 表
#### 実行条件と演算結果

| 実行条件 | 演算結果 |
| --- | --- |
| EN | ENO | (d) |
| TRUE（演算実行） | TRUE | 演算出力値 |
| FALSE（演算停止） | FALSE*1 | 不定値 |

*1 ENOからFALSEが出力された場合、(d)から出力されるデータは不定値となる。

#### エラーコード一覧

| エラーコード (SD0) | 内容 |
| --- | --- |
| 2820H | (s)で指定したデバイス番号以降、デバイス/ラベルメモリの各設定エリアに00Hが存在しないとき。 |
| 3405H | (s)の文字列が16383文字を超えるとき。<br>(n1)、(n2)に指定可能な範囲外のデータを設定したとき。<br>・(n1)、(n2)の値が0以下<br>・(n2)の値が有効値(-1、0、1以上)以外<br>・(n1)の値が(s)の文字数を超えている<br>・(n1)と(n2)を加算した値が(s)の文字数を超えている |
</contents>