ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page978

<overview>
このページは主にテキスト説明と表、簡単な図で構成されています。テキスト部分では、浮動小数点数の最小値検索機能の説明とその動作条件について記載されています。図はデータの配置例を示し、表はエラーコードとその内容をまとめています。グラフは含まれていません。
</overview>

<contents>
### テキスト
#### 機能
(s)から(n)点分の単精度実数のブロックデータに対して、最小値を検索し、(d)で指定したデバイスへ格納します。  
最初に最小値を検出したデータが、(s)から何点目かを(d)+2で指定したデバイスへ格納し、最小値と同じ値の個数を(d)+3で指定したデバイスへ格納します。  
検索結果（位置）の値については、(s)で指定したブロックデータの先頭を1点目として扱います。  

- (d),(d)+1: 最小値  
- (d)+2: 位置  
- (d)+3: 個数  

なお、(n)で指定した値が0の場合は無処理となります。

#### エラー
| エラーコード (SD0) | 内容 |
|--------------------|------|
| 3402H              | (s)で指定したブロックデータに、単精度実数以外の値が存在する場合。 |

### 図
- 左側の図は、(s)から始まる連続したデータの例を示しています。例えば、(s)+1に1.2345、(s)+3に123.45、(s)+5に-1.2345、(s)+7に-12.345、(s)+9に-123.45が格納されています。  
- 右側の図は、(d)から始まるデバイスに格納される結果を示しています。最小値-123.45が(d)+1に、最小値の位置5が(d)+2に、最小値と同じ値の個数1が(d)+3に格納される例です。

</contents>