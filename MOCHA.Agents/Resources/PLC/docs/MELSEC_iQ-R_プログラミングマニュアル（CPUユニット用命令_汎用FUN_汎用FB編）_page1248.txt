ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page1248

<overview>
本ページは「PID制御用データの設定」に関する説明で構成されています。テキスト部分ではPIDINIT命令の概要と使用上の注意が記載されており、ラダー図やFBD/LD図による命令の使用例が示されています。表部分では実行条件、設定データの内容・範囲・データ型、及び使用可能なデバイス一覧が詳細にまとめられています。グラフやチャートはなく、図は命令のラダー図とFBD/LD図のみです。
</overview>

<contents>
### テキスト
#### PID制御用データの設定
PIDINIT(P)命令は、プロセスCPU（二重化モード）およびSIL2プロセスCPUの両系実行プログラムで使用した場合、バックアップモード時の待機系で動作に制約がある旨の注意書きがあります（詳細はMELSEC iQ-R CPUユニットユーザーズマニュアル応用編参照）。

指定したデバイス番号以降に設定されている使用ループ数分のPID制御データを、一括でCPU内部に登録します。

#### ラダー図（ST）
- ENO:=PIDINIT(EN,s);
- ENO:=PIDINITP(EN,s);

#### FBD/LD図
- EN（入力）
- ENO（出力）
- s（PID制御用データが設定されている先頭デバイス）

### 表
#### 実行条件
| 命令     | 実行条件           |
| -------- | ------------------ |
| PIDINIT  | 立ち下がりエッジ   |
| PIDINITP | 立ち上がりエッジ   |

#### 設定データ
##### 内容、範囲、データ型
| オペランド | 内容                             | 範囲 | データ型 | データ型（ラベル） |
| ---------- | -------------------------------- | ---- | -------- | ------------------ |
| (s)        | PID制御用データが設定されている先頭デバイス | —    | ワード   | ANY16*1            |
| EN         | 実行条件                         | —    | ビット   | BOOL               |
| ENO        | 実行結果                         | —    | ビット   | BOOL               |

*1 ラベルで設定する場合は、動作に必要な領域が確保できるよう配列を定義し、その配列型ラベルの要素を指定する必要がある。

##### 使用可能デバイス
| オペランド | ビット                             | ワード                             | ダブルワード | 間接指定 | 定数 | その他 |
| ---------- | -------------------------------- | -------------------------------- | ------------ | -------- | ---- | ------ |
| (s)        | X, Y, M, L, SM, F, B, SB, FX, FY | J□¥□口<br>T, ST, C, D, W, SD, SW, FD, R, ZR, RD | U□¥G□, J□¥□口, U3E□¥(H)G□ | Z | LT, LST, LC | LZ | ○ | K, H | E | $ | — |

### 図
- ラダー図はPIDINIT命令の使用例を示し、sにPID制御用データの先頭デバイスを指定し、EN信号で実行、ENOで結果を返す構成。
- FBD/LD図は同様の機能をブロック図で表現し、EN入力、s入力、ENO出力の3端子構成。

</contents>