ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page510

<overview>
本ページは「任意データのビット分離」についての説明で構成されています。  
テキスト部分では、NDIS(P)命令の概要と動作説明、実行条件について記載されています。  
表部分では、命令のオペランド（パラメータ）の内容、範囲、データ型、使用可能なデバイスの種類が詳細に示されています。  
図部分には、ラダー図、FBD/LD図、命令の実行条件の波形図が掲載されており、命令の動作イメージを視覚的に理解できる構成です。
</overview>

<contents>
### テキスト
#### 任意データのビット分離
NDIS(P)命令は、指定したデータの各ビットを任意のビットずつに分離する命令です。

##### 対応CPU
RnCPU、RnENCPU、RnPCPU（プロセス、二重化）、RnPSFCPU（一般、安全）、RnSFCPU（一般、安全）で使用可能です。

##### 命令概要
- ラダー言語（ラダー）では、`ENO:=NDIS(EN,s1,s2,d);` または `ENO:=NDISP(EN,s1,s2,d);` の形式で記述します。
- FBD/LDでは、EN（実行条件）、s1（分離元データ）、s2（分離単位）、d（分離先データ）、ENO（実行結果）を持つブロックとして表現されます。

##### 実行条件
| 命令  | 実行条件の波形 |
|-------|----------------|
| NDIS  | 立下りエッジで実行 |
| NDISP | 立上りエッジで実行 |

### 表
#### 設定データ（オペランドの内容、範囲、データ型）

| オペランド | 内容                         | 範囲 | データ型           | データ型(ラベル) |
|------------|------------------------------|------|--------------------|------------------|
| (s1)       | 分離するデータが格納されている先頭デバイス | —    | 符号付きBIN16ビット | ANY16*1          |
| (d)        | 分離したデータを格納する先頭デバイス       | —    | 符号付きBIN16ビット | ANY16*1          |
| (s2)       | 分離単位を格納する先頭デバイス             | —    | 符号付きBIN16ビット | ANY16*1          |
| EN         | 実行条件                     | —    | ビット              | BOOL             |
| ENO        | 実行結果                     | —    | ビット              | BOOL             |

*1 ラベルで設定する場合は、動作に必要な領域が確保できる配列を定義し、その配列型ラベルの要素を指定する必要があります。

#### 使用可能デバイス

| オペランド | ビットデバイス                             | ワードデバイス                             | ダブルワードデバイス | 間接指定 | 定数 | その他 |
|------------|------------------------------------------|------------------------------------------|---------------------|----------|------|--------|
|            | X, Y, M, L, SM, F, B, SB, FX, FY         | J□￥□, T, ST, C, D, W, SD, SW, FD, R, ZR, RD | U□￥G□, J□￥□, U3E□￥(H)G□ | Z, LT, LST, LC | LZ       | K, H | E    | $      |
| (s1)       | —                                        | —                                        | ○                   | —        | —    | —      |
| (d)        | —                                        | —                                        | ○                   | —        | —    | —      |
| (s2)       | —                                        | —                                        | ○                   | —        | —    | —      |

### 図
- **ラダー図**  
  分離元データ(s1)、分離単位(s2)、分離先データ(d)を入力とし、NDIS命令が実行される構成を示しています。

- **FBD/LD図**  
  EN（実行条件）、s1、s2、d、ENO（実行結果）を持つ命令ブロックの形で表現されています。

- **実行条件の波形図**  
  NDIS命令は立下りエッジで実行されることを示す波形、NDISP命令は立上りエッジで実行されることを示す波形が掲載されています。
</contents>