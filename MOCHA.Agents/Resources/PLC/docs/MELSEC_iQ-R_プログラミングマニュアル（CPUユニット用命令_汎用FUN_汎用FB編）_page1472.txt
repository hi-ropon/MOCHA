ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page1472

<overview>
このページは文字列操作に関する機能説明の一部で、主に「LEFT(_E)」と「RIGHT(_E)」という演算処理の説明で構成されています。ページ全体は以下のセクションに分かれています。
- テキスト部分：LEFT(_E)、RIGHT(_E)の機能説明、入力値の範囲、演算結果の説明、エラーについての記述。
- 表部分：EN/ENO付きファンクションの実行条件と演算結果をまとめた表。
- 図部分：LEFT(_E)、RIGHT(_E)の動作例を示す図解（文字列の左側・右側から指定文字数分を抽出する様子をバイト単位で示している）。
</overview>

<contents>
### テキスト
#### 演算処理
- **LEFT(_E)**  
  入力された文字列(s)の左側から指定文字数(n)分のデータを(d)から出力します。  
  抽出する文字数は(n)の入力値により指定されます。

- **RIGHT(_E)**  
  入力された文字列(s)の右側から指定文字数(n)分のデータを(d)から出力します。  
  抽出する文字数は(n)の入力値により指定されます。

- (s)への入力値はSTRING型のデータ値で0～255バイトの範囲内です。  
- (n)への入力値はINT型のデータ値で0～255の範囲内です。ただし、(s)へ入力される文字列の文字数以内に限ります。

#### 演算結果
1. **EN/ENOなしファンクション**  
   演算処理を実行し、(d)から演算出力値が出力されます。

2. **EN/ENO付きファンクション**  
   実行条件と演算結果は以下の通りです。

| 実行条件 | 演算結果 |  
| :--- | :--- |  
| EN | ENO | (d) |  
| TRUE(演算実行) | TRUE | 演算出力値 |  
| FALSE(演算停止) | FALSE*1 | 不定値 |  

*1 ENOからFALSEが出力された場合、(d)から出力されるデータは不定値となります。その場合は(d)から出力されたデータを使用しないようプログラム処理を行う必要があります。

#### エラー
演算エラーはありません。

### 図
- **LEFT(_E)の例**  
  文字列「ABCDEF12345」(s)の左側から7文字を抽出し、(d)に「ABCDEF1」として出力する様子を示しています。  
  バイト単位で上位バイト・下位バイトに分けて表示し、抽出範囲を矢印と枠線で示しています。

- **RIGHT(_E)の例**  
  文字列「ABCDEF12345」(s)の右側から5文字を抽出し、(d)に「12345」として出力する様子を示しています。  
  同様にバイト単位で上位バイト・下位バイトに分けて表示し、抽出範囲を矢印と枠線で示しています。

### 表
#### EN/ENO付きファンクションの実行条件と演算結果

| 実行条件 | 演算結果 |  
| :--- | :--- |  
| EN | ENO | (d) |  
| TRUE(演算実行) | TRUE | 演算出力値 |  
| FALSE(演算停止) | FALSE*1 | 不定値 |  

*1 ENOがFALSEの場合、(d)の出力は不定値となるため注意が必要。
</contents>