ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page1095

<overview>
本ページは「時刻帯域比較出力（TZCP(P)）」に関する説明で構成されています。ページ全体は以下のセクションに分かれています。
・テキスト部分：TZCP命令の概要、対応CPU、使用条件の説明
・図部分：ラダー図、FBD/LD図による命令の動作イメージ
・表部分：実行条件のタイミング表、設定データの内容・範囲・データ型の表、使用可能デバイスの一覧表
グラフは含まれていません。
</overview>

<contents>
### テキスト
#### 時刻帯域比較出力（TZCP(P)）
TZCP命令は、下限値(s1)と上限値(s2)の時刻データの帯域に対し、比較する時刻データ(s3)を比較し、その結果に応じて(d)、(d)+1、(d)+2のいずれかがONになる命令です。

対応CPUはR00CPU、R01CPU、R02CPUでバージョン制限はなく、RnCPU（R00CPU、R01CPU、R02CPUを除く）およびRnENCPUはファームウェアバージョン17以降で使用可能です。エンジニアリングツールはバージョン1.020W以降で対応しています。

#### 実行条件
- TZCP命令は立下りエッジで実行
- TZCPP命令は立上りエッジで実行

### 図
#### ラダー図
- (s1)、(s2)、(s3)、(d)の4つの入力・出力を持つ命令ブロックの形状を示す。

#### FBD/LD図
- EN（実行条件）入力、ENO（実行結果）出力、s1、s2、s3の入力、dの出力を持つ命令ブロックの形状を示す。

### 表
#### 設定データ：内容、範囲、データ型

| オペランド | 内容 | 範囲 | データ型 | データ型(ラベル) |
| :--- | :--- | :--- | :--- | :--- |
| (s1) | 比較する時刻データ（時、分、秒）の下限値を格納する先頭デバイス | 機能詳細参照 | 符号付きBIN16ビット | ANY16_ARRAY（要素数：3） |
| (s2) | 比較する時刻データ（時、分、秒）の上限値を格納する先頭デバイス | 機能詳細参照 | 符号付きBIN16ビット | ANY16_ARRAY（要素数：3） |
| (s3) | 比較する時刻データ（時、分、秒）を格納する先頭デバイス | 機能詳細参照 | 符号付きBIN16ビット | ANY16_ARRAY（要素数：3） |
| (d) | 比較結果が格納される先頭デバイス | ― | ビット | ANYBIT_ARRAY（要素数：3） |
| EN | 実行条件 | ― | ビット | BOOL |
| ENO | 実行結果 | ― | ビット | BOOL |

#### 使用可能デバイス

| オペランド | ビット | ワード | ダブルワード | 間接指定 | 定数 | その他 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| (s1) | ― | ― | ― | ― | ○ | ― | ― |
| (s2) | ― | ― | ○ | ― | ― | ○ | ― | ― |
| (s3) | ― | ― | ○ | ― | ― | ○ | ― | ― |
| (d) | ○ | ― | ○*1 | ― | ― | ― | ― |

*1 T、ST、Cは使用不可。

---

以上の内容により、TZCP命令は指定した時刻の範囲内にあるかどうかを判定し、その結果をビットデバイスに出力する命令であることが理解できます。使用可能なデバイスや実行条件も明確に示されています。
</contents>