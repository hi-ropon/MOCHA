ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page1319

<overview>
本ページは「32.5 BOOL型→TIME型変換」についての説明で構成されています。  
テキスト部分では、関数の概要、設定データの説明、機能の詳細（演算処理と演算結果）、およびエラー情報が記載されています。  
表部分には、引数の内容・種別・データ型の一覧表と、EN/ENO付きファンクションの実行条件と演算結果の対応表が含まれています。  
図部分は、ラダー、FBD/LD、ST言語での関数の使用例を示すブロック図と、BOOL型からTIME型への変換イメージ図が掲載されています。
</overview>

<contents>
### テキスト
#### 32.5 BOOL型→TIME型変換
関数名：BOOL_TO_TIME(_E)  
BOOL型データをTIME型データへ変換する関数です。

##### 使用可能CPU
RnCPU、RnENCPU、RnPCPU（プロセス、二重化）、RnPSFCPU（一般、安全）

##### ラダー、FBD/LD、STでの使用例
- ラダー、FBD/LDではEN/ENOなしとEN/ENO付きの2種類のブロック図が示されています。  
- ST言語では、EN/ENOなしの場合は「d:=BOOL_TO_TIME(s);」、EN/ENO付きの場合は「d:=BOOL_TO_TIME_E(EN,ENO,s);」と記述します。

#### 設定データ
| 引数 | 内容 | 種別 | データ型 |
| :--- | :--- | :--- | :--- |
| EN | 実行条件(TRUE:実行、FALSE:停止) | 入力変数 | BOOL |
| s(IN) | 入力 | 入力変数 | BOOL |
| ENO | 出力状態(TRUE:正常実行、FALSE:異常または停止) | 出力変数 | BOOL |
| d | 出力 | 出力変数 | TIME |

#### 機能
##### 演算処理
- 入力されたBOOL型データ(s)をTIME型データ(d)に変換して出力します。  
- 入力値がFALSEの場合はTIME型のデータ値で0を出力します。  
- 入力値がTRUEの場合はTIME型のデータ値で1を出力します。

##### 演算結果
1. EN/ENOなしファンクション  
　演算処理を実行し、dから演算出力値が出力されます。  
2. EN/ENO付きファンクション  
　実行条件と演算結果は以下の通りです。

| 実行条件 EN | 演算結果 ENO | (d) |
| :--- | :--- | :--- |
| TRUE(演算実行) | TRUE | 演算出力値 |
| FALSE(演算停止) | FALSE*1 | 不定値 |

*1 ENOからFALSEが出力された場合、(d)から出力されるデータは不定値となります。その場合は(d)から出力されたデータを使用しないようプログラムで処理してください。

#### エラー
演算エラーはありません。

### 図
- ラダー、FBD/LDのブロック図  
　EN/ENOなしはs入力とd出力のみのシンプルなブロック。  
　EN/ENO付きはEN入力、ENO出力が追加されたブロック。  
- ST言語の記述例  
　EN/ENOなし：d:=BOOL_TO_TIME(s);  
　EN/ENO付き：d:=BOOL_TO_TIME_E(EN,ENO,s);  
- BOOL→TIME変換イメージ図  
　FALSE → T#0ms (TIME型0)  
　TRUE → T#1ms (TIME型1)  
　s（BOOL型）からd（TIME型）への変換を示す矢印付きの図。
</contents>