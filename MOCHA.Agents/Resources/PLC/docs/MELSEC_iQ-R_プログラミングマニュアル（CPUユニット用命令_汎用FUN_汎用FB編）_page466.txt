ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page466

<overview>
このページは「BIN16ビットデータブロック→BCD4桁データ変換ブロック」に関する説明で構成されています。  
主にテキスト説明、ラダー図とFBD/LD図の2種類の図、実行条件のタイミングを示す簡単な図、そして詳細な設定データをまとめた表が含まれています。  
テキスト部分では命令の概要と使用可能なデバイスの種類やデータ型について説明しています。  
図部分は命令のラダー回路とFBD/LD回路の構造を示し、表部分はオペランドの内容・範囲・データ型や使用可能なデバイスの詳細を示しています。
</overview>

<contents>
### テキスト
#### BIN16ビットデータブロック→BCD4桁データ変換ブロック
命令名：BKBCD(P)  
指定したデバイスから(n)点のBINデータ（0～9999）をBCD変換します。

### 図
#### ラダー図
- ラダー回路のシンボルが示されており、パラメータとして(s)、(d)、(n)が入力される構造です。  
- ST言語では以下のように記述されます。  
  ```
  ENO := BKBCD(EN, s, n, d);
  ENO := BKBCDP(EN, s, n, d);
  ```

#### FBD/LD図
- ブロック図でEN（Enable）、ENO（Enable Out）、s、d、nの各端子が示されています。

#### 実行条件
- BKBCD命令は立下りエッジで実行されます。  
- BKBCDP命令は立上りエッジで実行されます。

### 表
#### 設定データ：内容、範囲、データ型

| オペランド | 内容                         | 範囲       | データ型           | データ型（ラベル） |
|-------------|------------------------------|------------|--------------------|--------------------|
| (s)         | BINデータが格納されている先頭デバイス | —          | 符号付きBIN16ビット | ANY16*1            |
| (d)         | 変換後のBCDデータを格納する先頭デバイス | —          | BCD4桁             | ANY16*1            |
| (n)         | 変数データ数                 | 0～65535   | 符号なしBIN16ビット | ANY16              |
| EN          | 実行条件                   | —          | ビット             | BOOL               |
| ENO         | 実行結果                   | —          | ビット             | BOOL               |

*1 ラベルで設定する場合は、動作に必要な領域が確保できるよう配列を定義し、その配列型ラベルの要素を指定してください。

#### 使用可能デバイス

| オペランド | ビット                             | ワード                                     | ダブルワード | 間接指定 | 定数 | その他 |
|-------------|----------------------------------|--------------------------------------------|--------------|----------|------|--------|
| (s)         | —                                | ○（T, ST, C, D, W, SD, SW, FD, R, ZR, RD） | —            | —        | ○    | —      |
| (d)         | —                                | ○（T, ST, C, D, W, SD, SW, FD, R, ZR, RD） | —            | —        | —    | —      |
| (n)         | ○（X, Y, M, L, SM, F, B, SB, FX, FY） | ○（J□￥□, U□￥G□, J□￥□, U3E□￥(H)G□） | ○（Z）      | —        | ○    | —      |

※表中の記号は使用可能なデバイスの種類を示しています。

</contents>