ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page70

<overview>
本ページは命令コードに関する説明で構成されており、主にテキストと表で構成されています。テキスト部分では命令の種類（BIN16ビット加減算、BIN32ビット加減算、BIN16ビット乗除算）が見出しとして示され、それぞれの命令に対応する命令記号、処理内容、参照先をまとめた表が配置されています。グラフや図は含まれていません。
</overview>

<contents>
### テキスト
#### 算術演算命令
本ページではBIN16ビットおよびBIN32ビットの加減算命令、さらにBIN16ビットの乗除算命令について説明しています。

### 表
#### BIN16ビット加減算命令
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| +, +P, +_U, +P_U | 指定した2つのBIN16ビットデータを加算します。（オペランドが2個の場合） | 232ページ +P(_U) [オペランドが2個の場合] |
| +, +P, +_U, +P_U | 指定した2つのBIN16ビットデータを加算します。（オペランドが3個の場合） | 234ページ +P(_U) [オペランドが3個の場合] |
| -, -P, -_U, -P_U | 指定した2つのBIN16ビットデータを減算します。（オペランドが2個の場合） | 236ページ -P(_U) [オペランドが2個の場合] |
| -, -P, -_U, -P_U | 指定した2つのBIN16ビットデータを減算します。（オペランドが3個の場合） | 238ページ -P(_U) [オペランドが3個の場合] |

#### BIN32ビット加減算命令
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| D+, D+P, D+_U, D+P_U | 指定した2つのBIN32ビットデータを加算します。（オペランドが2個の場合） | 240ページ D+P(_U) [オペランドが2個の場合] |
| D+, D+P, D+_U, D+P_U | 指定した2つのBIN32ビットデータを加算します。（オペランドが3個の場合） | 242ページ D+P(_U) [オペランドが3個の場合] |
| D-, D-P, D-_U, D-P_U | 指定した2つのBIN32ビットデータを減算します。（オペランドが2個の場合） | 244ページ D-P(_U) [オペランドが2個の場合] |
| D-, D-P, D-_U, D-P_U | 指定した2つのBIN32ビットデータを減算します。（オペランドが3個の場合） | 246ページ D-P(_U) [オペランドが3個の場合] |

#### BIN16ビット乗除算命令
| 命令記号 | 処理内容 | 参照先 |
| :--- | :--- | :--- |
| *, *P, *_U, *P_U | 指定した2つのBIN16ビットデータを乗算します。 | 248ページ *(P)(_U) |
| /, /P, /_U, /P_U | 指定した2つのBIN16ビットデータを除算します。 | 250ページ /(P)(_U) |
</contents>