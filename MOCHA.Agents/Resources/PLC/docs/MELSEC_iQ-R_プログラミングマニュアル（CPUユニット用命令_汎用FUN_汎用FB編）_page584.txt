ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page584

<overview>
このページは「FCALL(P)命令」の機能説明に関する内容で構成されています。ページ全体は主にテキスト部分で説明がなされており、命令の動作概要、非実行処理の説明、及び非実行処理後の各コイル命令の演算結果についての表が含まれています。また、簡単な図が1つあり、メインルーチンプログラムとサブルーチンプログラムの関係を示しています。
</overview>

<contents>
### テキスト
#### 機能
- FCALL(P)命令を実行すると、ポインタ(P)のサブルーチンプログラムの非実行処理を行います。
- FCALL(P)命令は、同一プログラムファイル内のポインタで指定されたサブルーチンプログラムと、グローバルポインタで指定されたサブルーチンプログラムを非実行にできます。
- 非実行処理とは、各コイル命令を条件設定がOFF状態と同一の処理を行うことを指します。

#### 非実行処理後の各コイル命令の演算結果
- 非実行処理後の各コイル命令の演算結果は、条件接点のON/OFFに関係なく以下の通りとなります。

| 演算で使用するデバイス                         | 演算結果（デバイスの状態）               |
|----------------------------------------------|----------------------------------------|
| 高速タイマ、低速タイマ                         | 0となります。                           |
| 高速積算タイマ、低速積算タイマ、カウンタ       | 現在の状態を保持します。                 |
| OUT命令中のデバイス                            | 強制的にOFFとなります。                   |
| SET命令、RST命令中のデバイス、SFT(P)命令中のデバイス、基本/応用命令中のデバイス | 現在の状態を保持します。                 |
| PLS命令、パルス化命令（□P）                   | 条件接点OFFと同一処理となります。         |

- FCALL(P)命令はCALL(P)命令と組み合わせて使用します。FCALL(P)命令をCALL(P)命令と組み合わせて実行しない場合は、実行指令をOFFにしてもサブルーチンプログラムの非実行処理は行われず、各コイル命令の出力状態を保持します。

### 図
- メインルーチンプログラム（A）からサブルーチンプログラム（B）へポインタ(P)で呼び出し、FCALL(P)命令の指令がON→OFF時に非実行処理を行う流れを示す簡単なフローチャート。
- メインルーチンプログラム内にFCALL(P)命令があり、サブルーチンプログラム内にRET命令がある構成。

</contents>