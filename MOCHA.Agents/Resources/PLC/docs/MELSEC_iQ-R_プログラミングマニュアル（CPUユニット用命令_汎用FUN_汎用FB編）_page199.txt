ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page199

<overview>
このページはテキスト部分と図（ラダー図）で構成されています。テキスト部分では、入れ子構造の制御に関する注意事項が記載されており、図はMC命令とMCR命令の入れ子構造の例を示しています。図は2つのラダー図が並べられており、左側が入れ子構造の一般的な例、右側が最も若いネスティング番号を用いた場合の例を表しています。
</overview>

<contents>
### テキスト
#### 注意事項
- 入れ子は15個（N0～N14）まで可能です。
- 入れ子にする場合、MC命令ではネスティング(N)の若番から用いていき、MCR命令は老番から用います。
- MCR命令が1箇所に集まっている入れ子構造のときは、最も若いネスティング(N)の番号1つで、すべてのマスタコントロールを終了させることができます。

### 図（ラダー図）
- 左側の図は、X1、X2、X3の入力に対して、それぞれMC命令（MC N0 M15、MC N1 M16、MC N2 M17）とMCR命令（MCR N2、MCR N1、MCR N0）が階層的に配置されている入れ子構造の例を示しています。
- 右側の図は、同じ入力X1、X2、X3に対してMC命令は同じですが、MCR命令は最も若いネスティング番号N0のみを用いてマスタコントロールを終了させている例を示しています。
- これにより、MCR命令を1つにまとめて効率的に制御できることを視覚的に表現しています。
</contents>