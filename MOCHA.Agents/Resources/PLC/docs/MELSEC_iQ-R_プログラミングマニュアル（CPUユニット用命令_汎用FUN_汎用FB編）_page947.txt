ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page947

<overview>
本ページは「倍精度実数ラジアン→角度変換」命令(DEGD(P))の説明で構成されています。ページ全体は以下のセクションに分かれています。
- テキスト部分：命令の概要説明、使用可能なCPU種別、命令の動作説明
- 図部分：ラダー図、FBD/LD図による命令の使用例
- 表部分：実行条件の説明表、設定データの内容・範囲・データ型の表、使用可能デバイスの一覧表
グラフ部分はありません。
</overview>

<contents>
### テキスト
#### 倍精度実数ラジアン→角度変換（DEGD(P)）
この命令は、角度の大きさの単位を倍精度実数で指定したラジアン単位から度単位に変換します。  
対応CPUはRnCPU、RnENCPU、RnPCPU（プロセス、二重化）、RnPSFCPU（一般、安全）となっています。

### 図
#### ラダー図
- ラダー図では、(s)にラジアン角度を指定し、(d)に度単位に変換した値を格納します。
- ST命令例：  
  ENO:=DEGD(EN,s,d);  
  ENO:=DEGDP(EN,s,d);

#### FBD/LD図
- ブロック図では、EN（実行条件）、s（ラジアン角度）、d（度単位角度）、ENO（実行結果）を示しています。

### 表
#### 実行条件
| 命令  | 実行条件の波形例 |
|-------|------------------|
| DEGD  | 立ち下がりパルス  |
| DEGDP | 立ち上がりパルス  |

#### 設定データ（内容、範囲、データ型）
| オペランド | 内容                                   | 範囲                         | データ型       | データ型(ラベル) |
|------------|--------------------------------------|------------------------------|----------------|------------------|
| (s)        | 度単位に変換するラジアン角度または、ラジアン角度が格納されている先頭デバイス | 0, 2^-1022 ≤ |s| < 2^1024 | 倍精度実数      | ANYREAL_64       |
| (d)        | 度単位に変換した値を格納する先頭デバイス | —                            | 倍精度実数      | ANYREAL_64       |
| EN         | 実行条件                             | —                            | ビット          | BOOL             |
| ENO        | 実行結果                             | —                            | ビット          | BOOL             |

#### 使用可能デバイス
| オペランド | ビット                             | ワード                                   | ダブルワード               | 間接指定 | 定数 | その他 |
|------------|----------------------------------|-----------------------------------------|----------------------------|----------|------|--------|
| (s)        | —                                | T, ST, C, D, W, SD, SW, FD, R, ZR, RD  | Z, LT, LST, LC             | ○        | —    | ○      |
| (d)        | —                                | —                                       | Z                          | ○        | —    | —      |

- ビットデバイスは使用不可。
- ワードデバイスは(s)のみ使用可能。
- ダブルワードデバイスは(s)(d)ともに使用可能。
- 間接指定は(s)(d)ともに可能。
- 定数は使用不可。
- その他は(s)のみ使用可能。

以上の内容により、倍精度実数のラジアン角度を度単位に変換する命令の使い方、条件、対応デバイスが詳細に説明されています。
</contents>