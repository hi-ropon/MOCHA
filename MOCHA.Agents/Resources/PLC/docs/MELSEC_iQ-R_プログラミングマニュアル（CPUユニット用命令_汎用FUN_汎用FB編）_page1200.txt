ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page1200

<overview>
このページは、FTPサーバへファイルを転送するプログラムのラダー図とその説明で構成されています。テキスト部分では、FTPサーバの指定方法やプログラムの動作概要が記載されています。表やグラフはなく、ラダー図（プログラムの命令構成）が図として示されています。
</overview>

<contents>
### テキスト
・SP.FTPPUT命令のコントロールデータでFTPサーバを指定する場合、X0をONにすると、CPUユニットのドライブ2に格納されているファイル「\database1\recipe\recipe_db.txt」をFTPサーバアドレス「192.168.3.30」の「\database1\recipe\」に送付するプログラムであることが説明されています。

### 図（ラダー図）
このラダー図は、FTPサーバへファイル転送を行うための命令列を示しています。各命令の役割は以下の通りです。

1. 使用用途設定エリアを設定（MOVP H8008 → D0）
2. FTPサーバを設定（MOVP H0 → D4）
3. FTPサーバアドレスを設定（DMOVP H0C0A8031E → D134）
4. ログイン名を設定（$MOVP "USER001" → D136）
5. パスワードを設定（$MOVP "Pw@12345678901" → D153）
6. FTPサーバの接続方式を設定（MOVP H1 → D170）
7. ポート番号を設定（MOVP K21 → D171）
8. SP.FTPPUT命令を実行（SP.FTPPUT U0 D0 "2:\database1\recipe\recipe_db.txt" "\database1\recipe" → M100）
9. 正常完了を表示（SET M150）
10. 異常完了を表示（SET M151）

このプログラムは、X0がONになるとFTPサーバへのファイル転送処理を開始し、転送完了後に正常または異常の完了フラグをセットします。
</contents>