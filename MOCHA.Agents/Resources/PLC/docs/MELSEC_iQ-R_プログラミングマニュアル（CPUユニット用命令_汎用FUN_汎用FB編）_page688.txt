ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page688

<overview>
本ページは「8.5 データ制御命令」の一部で、「BIN16ビットデータ上下限リミット制御」について説明しています。ページ構成は以下の通りです。
- テキスト部分：命令の概要説明、実行条件の説明、注意事項
- 図部分：ラダー図、FBD/LDの命令ブロック図
- 表部分：命令の実行条件を示すタイミング図、設定データの内容・範囲・データ型をまとめた表
グラフ部分はありません。
</overview>

<contents>
### テキスト
#### 8.5 データ制御命令 BIN16ビットデータ上下限リミット制御
「LIMIT(P)(_U)」命令は、指定した入力値（BIN16ビット値）が上下限リミット値の範囲内か否かにより、出力値を制御します。

- 対応CPU：RnCPU、RnENCPU、RnPCPU（プロセス、二重化）、RnPSFCPU（一般、安全）
- 注意：LIMIT命令、LIMIT_U命令はST、FBD/LDでは対応していません。汎用ファンクションのLIMITを使用してください（1459ページ参照）。

#### 実行条件
命令ごとに実行条件のタイミングが異なり、図で示されています。
- LIMIT、LIMIT_U：立下りエッジで実行
- LIMITP、LIMITP_U：立上りエッジで実行

### 図
- ラダー図：入力(s1,s2,s3,d)の接続イメージと、ST命令での使用例（ENO:=LIMITP(EN,s1,s2,s3,d); ENO:=LIMITP_U(EN,s1,s2,s3,d);）
- FBD/LD図：命令ブロックの入出力端子（EN, ENO, s1, s2, s3, d）

### 表
#### 設定データ（内容、範囲、データ型）

| オペランド | 内容 | 範囲 | データ型 | データ型（ラベル） |
| :--- | :--- | :--- | :--- | :--- |
| (s1) | LIMIT(P) 下限リミット値（最小出力限界値） | -32768～32767 (符号付きBIN16ビット) / 0～65535 (符号なしBIN16ビット) | 符号付きBIN16ビット / 符号なしBIN16ビット | ANY16_S / ANY16_U |
| (s2) | LIMIT(P) 上限リミット値（最大出力限界値） | 同上 | 同上 | 同上 |
| (s3) | LIMIT(P) 上下限リミット制御により制御する入力値 | 同上 | 同上 | 同上 |
| (d) | LIMIT(P) 上下限リミット制御により制御した出力値を格納するデバイス | — | 符号付きBIN16ビット / 符号なしBIN16ビット | ANY16_S / ANY16_U |
| EN | 実行条件 | — | ビット | BOOL |
| ENO | 実行結果 | — | ビット | BOOL |

</contents>