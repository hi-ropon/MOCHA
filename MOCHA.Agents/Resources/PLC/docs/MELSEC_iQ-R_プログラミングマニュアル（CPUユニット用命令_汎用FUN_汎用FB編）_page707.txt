ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page707

<overview>
このページは、スケーリング用変換データの機能説明、設定項目の表、スケーリングの概念を示すグラフ、注意事項、及びエラーコード一覧で構成されています。テキスト部分では機能の概要と注意点が説明され、表部分では座標ポイント数とデバイス割付の対応関係が示されています。グラフ部分はX-Y座標のポイント配置と演算範囲を視覚的に表現しています。最後にエラーコードとその内容が表形式でまとめられています。
</overview>

<contents>
### テキスト
#### 機能
(s2)で指定した16ビット単位のスケーリング用変換データに対し、(s1)で指定した入力値からスケーリングを行い、(d)で指定されたデバイス番号に演算結果を格納します。スケーリング変換は、(s2)で指定したデバイス以降に格納したスケーリング用変換データに基づいて実施されます。

- 演算結果が整数値にならない場合は、小数点以下1桁目を四捨五入します。
- スケーリング用変換データのX座標データは昇順に設定してください。
- (s1)はスケーリング用変換データ範囲内((s2)のデバイス値)で設定してください。
- 複数のポイントが同一X座標の場合は、ポイントNo.が最大のY座標値を出力します。
- スケーリング用変換データの座標ポイント数は1～65535にしてください。

#### 注意事項
スケーリング用変換データが昇順にソートされている場合、SM755（スケーリングデータチェック設定）の状態により探索方法が異なり、処理速度も変わります。詳細はSCL(P)(_U)命令の700ページを参照してください。

### 表
#### 設定項目（nは(s2)で指定する座標ポイント数）
| 座標ポイント数 | デバイス割付 |
| :--- | :--- |
| X座標 ポイント1 | (s2)+1 |
| X座標 ポイント2 | (s2)+2 |
| ... | ... |
| X座標 ポイントn | (s2)+n |
| Y座標 ポイント1 | (s2)+n+1 |
| Y座標 ポイント2 | (s2)+n+2 |
| ... | ... |
| Y座標 ポイントn | (s2)+2n |

### グラフ
X軸に入力値(s1)、Y軸に演算結果(d)を示す座標系のグラフです。X座標のポイント1～nに対応するY座標ポイント1～nがプロットされており、ポイント間は線で結ばれています。X軸の範囲外は演算エラーとなり、演算可能範囲内でスケーリングが行われます。

### エラー
| エラーコード (SD0) | 内容 |
| :--- | :--- |
| 3405H | ・X座標データが昇順に設定されていない場合<br>・(s1)で指定した入力値が設定したスケーリング用変換データの範囲外の場合<br>・(s2)のデバイスから座標ポイント数が1～65535の範囲外の場合 |

</contents>