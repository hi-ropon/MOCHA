ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page919

<overview>
本ページは「倍精度実数COS演算」についての説明で構成されています。ページ全体は以下のセクションで構成されています。
- テキスト部分：COSD(P)命令の概要説明、実行条件の説明
- 図部分：ラダー図、FBD/LD図の命令表現例
- 表部分：実行条件の命令一覧表、設定データの内容・範囲・データ型の表、使用可能デバイスの一覧表
グラフ部分はありません。
</overview>

<contents>
### テキスト
#### 倍精度実数COS演算 COSD(P)
倍精度実数で指定した角度のCOS（余弦）値を演算します。

#### 実行条件
COSD命令には2種類あり、それぞれの実行条件は以下の通りです。
- COSD：パルス信号で実行
- COSDP：立ち上がりエッジで実行

#### 注意事項
COSD命令はST、FBD/LDでは対応していません。汎用ファンクションのCOSを使用してください（1423ページ参照）。

---

### 図
#### ラダー図
COSD命令のラダー表現例が示されています。引数として(s)と(d)があり、COSDP命令はST言語で「ENO:=COSDP(EN,s,d);」と記述されます。

#### FBD/LD図
COSD命令のFBD/LD表現例が示されています。EN（実行条件）、ENO（実行結果）、s（角度データ）、d（演算結果格納先）を入力・出力するブロック図です。

---

### 表
#### 実行条件一覧

| 命令  | 実行条件       |
|-------|----------------|
| COSD  | パルス信号     |
| COSDP | 立ち上がりエッジ |

#### 設定データ：内容、範囲、データ型

| オペランド | 内容                             | 範囲                         | データ型       | データ型(ラベル) |
|------------|--------------------------------|------------------------------|----------------|------------------|
| (s)        | COS(余弦)演算を行う角度データまたは角度データが格納されている先頭デバイス | 0, 2^-1022 ≤ (s) < 2^1024    | 倍精度実数     | ANYREAL_64       |
| (d)        | 演算結果を格納する先頭デバイス | —                            | 倍精度実数     | ANYREAL_64       |
| EN         | 実行条件                       | —                            | ビット         | BOOL             |
| ENO        | 実行結果                       | —                            | ビット         | BOOL             |

#### 使用可能デバイス

| オペランド | ビット                                     | ワード                                             | ダブルワード           | 間接指定 | 定数       | その他 |
|------------|--------------------------------------------|----------------------------------------------------|------------------------|----------|------------|--------|
| (s)        | —                                          | ○ (T, ST, C, D, W, SD, SW, FD, R, ZR, RD)          | —                      | —        | ○          | —      |
| (d)        | —                                          | ○                                                  | —                      | ○        | —          | —      |

---

以上より、本ページは倍精度実数のCOS演算命令COSD(P)の使い方、実行条件、入力・出力データの仕様、対応デバイスについて詳細に説明しています。
</contents>