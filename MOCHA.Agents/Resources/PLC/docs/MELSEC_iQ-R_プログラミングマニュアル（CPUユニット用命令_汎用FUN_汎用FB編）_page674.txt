ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page674

<overview>
本ページは「指定ファイルの移動」命令SP.FMOVEに関する説明で構成されています。ページ全体はテキスト説明、ラダー図とFBD/LD図の2種類の回路図、実行条件の簡単な表、そして詳細な設定データの表で構成されています。グラフやチャートはなく、図は命令のラダー回路とFBD/LD回路の構造を示すものです。
</overview>

<contents>
### テキスト
#### 指定ファイルの移動（SP.FMOVE）
SP.FMOVE命令は、SDメモリカード内の指定したファイルまたはフォルダを移動する命令です。フォルダ指定の場合はフォルダごと、または指定フォルダ内のすべてのファイルやサブフォルダを移動します。

対応CPUは以下の通りで、バージョン制限があります。
- R00CPUでは使用不可。
- R01CPU、R02CPUはファームウェアバージョンが"08"以降で使用可能。
- RnCPU（R00CPU、R01CPU、R02CPUを除く）、RnENCPUはファームウェアバージョンが"40"以降で使用可能。
- エンジニアリングツールはバージョン"1.050C"以降で使用可能。

### 図
#### ラダー図
- (U)、(d1)、(s1)、(s2)、(s3)、(s4)、(d2)の7つの入力・出力を持つ命令ブロックが示されている。
- ST言語では、`ENO:=SP_FMOVE(EN,U,s1,s2,s3,s4,d1,d2);`と記述。

#### FBD/LD図
- EN、U、s1、s2、s3、s4の入力と、ENO、d1、d2の出力を持つ命令ブロックの図。

### 表
#### 実行条件
| 命令    | 実行条件 |
|---------|----------|
| SP.FMOVE | ENがONの時 |

#### 設定データ（内容、範囲、データ型）
| オペランド | 内容                                   | 範囲                                   | データ型       | データ型（ラベル）          |
|------------|--------------------------------------|--------------------------------------|----------------|-----------------------------|
| (U)        | ダミー                               | －                                   | デバイス名     | ANY16                       |
| (d1)       | コントロールデータが格納されている先頭デバイス | 675ページ コントロールデータ(d1)参照 | ワード         | ANY16_ARRAY（要素数:2）      |
| (s1)       | 移動元ドライブ指定                   | 2（固定）*1                          | ワード         | ANY16                       |
| (s2)       | 移動元ファイル名/フォルダ名が格納されている先頭デバイス | －                                   | Unicode文字列  | ANYSTRING_DOUBLE            |
| (s3)       | 移動先ドライブ指定                   | 2（固定）*1                          | ワード         | ANY16                       |
| (s4)       | 移動先フォルダパスを格納するデバイス | －                                   | Unicode文字列  | ANYSTRING_DOUBLE            |
| (d2)       | 処理完了にてONするビットデバイス     | －                                   | ビット         | ANYBIT_ARRAY（要素数:2）     |
| EN         | 実行条件                             | －                                   | ビット         | BOOL                        |
| ENO        | 実行結果                             | －                                   | ビット         | BOOL                        |

*1 SDメモリカードのドライブ2のみ設定可能。

</contents>