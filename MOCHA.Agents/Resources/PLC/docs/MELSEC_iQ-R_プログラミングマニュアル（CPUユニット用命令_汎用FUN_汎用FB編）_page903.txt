ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page903

<overview>
このページは「単精度実数データ転送」命令EMOV(P)についての説明で構成されています。ページ全体は以下のセクションに分かれています。
- テキスト部分：命令の概要説明、実行条件、機能説明、エラー情報
- 表部分：設定データの内容・範囲・データ型、使用可能デバイスの一覧
- 図部分：ラダー図、FBD/LD図、実行条件のタイミング図、単精度実数データ転送のイメージ図
</overview>

<contents>
### テキスト
#### 単精度実数データ転送 EMOV(P)
単精度実数データを指定されたデバイスへ転送する命令です。

#### 実行条件
- EMOV命令は立下りエッジで実行されます。
- EMOVP命令は立上りエッジで実行されます。

#### 機能
(s)で指定されたデバイスに格納されている単精度実数データを、(d)で指定されたデバイスへ転送します。

#### エラー
演算エラーはありません。

---

### 図
- ラダー図：EMOV命令のラダー回路表現。EN入力、s（ソース）、d（デスティネーション）、ENO出力が示されています。
- FBD/LD図：機能ブロック図でEN、ENO、s、dの接続が示されています。
- 実行条件図：EMOVは立下りエッジ、EMOVPは立上りエッジで命令が実行されるタイミングを示す波形図。
- 単精度実数データ転送イメージ図：s+1番地にある単精度実数データ4.23542が転送され、d+1番地に同じ値が格納される様子を示しています。

---

### 表
#### 設定データ（内容、範囲、データ型）

| オペランド | 内容                                   | 範囲                | データ型     | データ型(ラベル) |
|-------------|--------------------------------------|---------------------|--------------|------------------|
| (s)         | 転送するデータまたは格納されている先頭デバイス | 0, 2^-126 ≤ |s| < 2^128 | 単精度実数   | ANYREAL_32       |
| (d)         | 転送先のデータを格納する先頭デバイス         | —                   | 単精度実数   | ANYREAL_32       |
| EN          | 実行条件                               | —                   | ビット       | BOOL             |
| ENO         | 実行結果                               | —                   | ビット       | BOOL             |

#### 使用可能デバイス

| オペランド | ビット                         | ワード                                   | ダブルワード       | 間接指定 | 定数 | その他 |
|-------------|------------------------------|----------------------------------------|--------------------|----------|------|--------|
| (s)         | —                            | T, ST, C, D, W, SD, SW, FD, R, ZR, RD | U□￥G□, J□￥口, U3E□￥(H)G□ | Z, LT, LST, LC, LZ | ○        | —    | —      |
| (d)         | —                            | —                                      | ○                  | ○        | —    | —      |

※ビットデバイスは使用不可、ワード・ダブルワードデバイスが使用可能。

---

以上が単精度実数データ転送命令EMOV(P)の詳細説明です。
</contents>