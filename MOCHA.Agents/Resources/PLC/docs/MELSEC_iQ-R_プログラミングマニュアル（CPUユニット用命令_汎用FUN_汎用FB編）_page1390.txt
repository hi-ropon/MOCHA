ファイル名: MELSEC_iQ-R_プログラミングマニュアル（CPUユニット用命令_汎用FUN_汎用FB編）_page1390

<overview>
本ページは「32.54 STRING型→WORD型変換」についての説明で構成されています。  
ページ全体は以下のセクションに分かれています。  
・テキスト部分：関数の概要説明、引数の説明、機能説明、演算結果の説明、エラー説明  
・図部分：ラダー回路やST言語での関数使用例の簡単な図示  
・表部分：引数の内容・種別・データ型の表、演算結果の条件と結果の表、エラーコードと内容の表  
グラフは含まれていません。
</overview>

<contents>
### テキスト
#### 32.54 STRING型→WORD型変換
STRING_TO_WORD(_E)関数は、STRING型データをWORD型データへ変換します。

#### ラダー、FBD/LD、STでの使用例
- ラダー/FBD/LDでは、EN/ENOなしとEN/ENO付きの2種類のブロック図が示されています。  
- ST言語では、EN/ENOなしの場合は「d:=STRING_TO_WORD(s);」、EN/ENO付きの場合は「d:=STRING_TO_WORD_E(EN,ENO,s);」と記述します。

#### 設定データ
| 引数 | 内容 | 種別 | データ型 |
| :--- | :--- | :--- | :--- |
| EN | 実行条件(TRUE:実行、FALSE:停止) | 入力変数 | BOOL |
| s(IN) | 入力 | 入力変数 | STRING(4) |
| ENO | 出力状態(TRUE:正常、FALSE:異常) | 出力変数 | BOOL |
| d | 出力 | 出力変数 | WORD |

#### 機能
- 演算処理  
  (s)に入力されたSTRING型のデータをWORD型のデータに変換し、(d)から出力します。  
  (s)への入力値はSTRING型のデータ値です。

#### 演算結果
1. EN/ENOなしファンクション  
   演算処理を実行し、(d)から演算出力値が出力されます。  
2. EN/ENO付きファンクション  
   実行条件と演算結果は以下の通りです。

| 実行条件 EN | 演算結果 ENO | 演算結果 (d) |
| :--- | :--- | :--- |
| TRUE(演算実行) | TRUE | 演算出力値 |
| FALSE(演算停止) | FALSE*1 | 不定値 |

*1 ENOからFALSEが出力された場合、(d)から出力されるデータは不定値となります。その場合は(d)から出力されたデータを使用しないようプログラム処理を行ってください。

#### エラー
| エラーコード (SD0) | 内容 |
| :--- | :--- |
| 3401H | 入力の各桁のアスキーコードが、30H～39H、41H～46H以外のとき。 |

### 図
- ラダー/FBD/LDの関数ブロック図  
  EN/ENOなしは単純にsからdへ変換するブロック。  
  EN/ENO付きはEN入力、ENO出力が付加されたブロック。  
- ST言語の関数呼び出し例も示されている。

### 表
- 引数の内容、種別、データ型の表（上記参照）  
- 実行条件と演算結果の対応表（上記参照）  
- エラーコードと内容の表（上記参照）

</contents>