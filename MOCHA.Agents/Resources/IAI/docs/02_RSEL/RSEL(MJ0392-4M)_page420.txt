ファイル名: RSEL(MJ0392-4M)_page420

<overview>
本ページは「6.3 パラメーター設定」の一部で、主に入出力ポートの割り当て設定に関する説明が中心です。ページ構成は以下の通りです。
- テキスト部分：使用例の説明、入出力ポートの割り当て範囲の説明
- 画像部分：ハードウェアの入出力ポート位置と割り当て番号の図解
- 表部分：パラメーター名、出荷時設定値、設定値、単位、備考をまとめた詳細なパラメーター一覧表
グラフやフローチャートは含まれていません。
</overview>

<contents>
### テキスト
#### 使用例②
入出力ポートの先頭（No.0～、300～）から、EtherCATを入出力各192点、PIOを入出力各48点連続で使用する場合の設定例です。
この場合、PIOはPIOユニット3台（16点×3台＝48点）使用し、入出力ポートはNo.192～、492～となります。

### 画像説明
- 左側のポートは「入力ポート No.0～191」「出力ポート No.300～491」
- 右側の3台のPIOユニット部分は「入力ポート No.192～239」「出力ポート No.492～539」

### 表
| No. | パラメーター名称                         | 出荷時設定値 | 設定値   | 単位 | 備考                                                                                           |
|-----|-----------------------------------------|--------------|----------|------|------------------------------------------------------------------------------------------------|
| 1   | 入出力ポート割付け種別                   | 0            | 1        |      | 0:固定割付け<br>1:自動割付け<br>※自動割付け時ポート割付け優先順位（I/O1→PIOユニット・PIO/SIOユニット） |
| 14  | I/O1 フィールドバスリモート入力使用ポート数 | 256          | 192      |      | 8の倍数                                                                                       |
| 15  | I/O1 フィールドバスリモート出力使用ポート数 | 256          | 192      |      | 8の倍数                                                                                       |
| 16  | I/O1 固定割付け時入力ポート開始 No.       | 0            | -1       |      | 0 +（8の倍数）（0～299）<br>1,000 +（8の倍数）（1,000～3,999）<br>（マイナス時無効）             |
| 17  | I/O1 固定割付け時出力ポート開始 No.       | 300          | -1       |      | 300 +（8の倍数）（300～599）<br>4,000 +（8の倍数）（4,000～6,999）<br>（マイナス時無効）         |
| 18  | I/O1 異常監視                           | 1            | 1        |      | 0:非監視<br>1:監視<br>※一部例外あり                                                        |
| 20  | 入力フィルタリング周期                     | 2            | 任意     | ms   | 入力信号は、本パラメーターの2倍時間の状態保持により認識される。<br>※PIO、PIOユニット・PIO/SIOユニットのみ有効 |
| 120 | ネットワーク属性1                        | 641001₁₆    | 任意     |      | ビット4-11:I/O1 フィールドバスリンク異常確認タイマー値（10ms）<br>ビット28-31:I/O1 フィールドバスリンク異常時入力ポートデータ選択（0:クリアー、1:ホールド） |
| 121 | ネットワーク属性2                        | C80000₁₆    | 任意     |      | ビット16-27:フィールドバス初期化時リンクタイムアウト値（100ms）<br>ビット28-31:EtherCAT PDOマッピング種別（0:可変、1:32Byte固定） |

</contents>