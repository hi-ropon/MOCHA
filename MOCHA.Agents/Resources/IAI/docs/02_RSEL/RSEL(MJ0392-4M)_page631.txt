ファイル名: RSEL(MJ0392-4M)_page631

<overview>
本ページは「リニアアブソ原点プリセット値（パラメーターNo.150）」に関する説明で構成されています。  
構成は以下の通りです。  
- テキスト部分：パラメーターの概要説明、注意事項  
- 表部分：パラメーターの詳細（No.、名称、単位、入力範囲、初期値）  
- 図部分：スライダー1とスライダー2の位置関係やストローク、リミットなどの関係を示した模式図  
</overview>

<contents>
### テキスト
#### リニアアブソ原点プリセット値（パラメーターNo.150）
擬似アブソ仕様のアクチュエーターの場合、原点位置を設定します。  
基準（工場出荷時の初期位置）に対するそれぞれの位置関係を図で示しています。

#### 注意
- 必ず設定前に工場出荷時の初期値を控えてください。  
- 工場出荷時の初期値を基準として設定を行ってください。  
- 設定後、必ず低速で動作確認を行ってください。  
- 擬似アブソ仕様のアクチュエーターの場合、No.22：原点復帰オフセット量およびNo.139：原点プリセット値は無効となります。

### 表
#### リニアアブソ原点プリセット値（パラメーターNo.150）

| No.  | 名称                     | 単位 | 入力範囲               | 工場出荷時の初期値         |
| :--- | :----------------------- | :--- | :--------------------- | :------------------------- |
| 150  | リニアアブソ原点プリセット値 | mm   | -9,999.99～9,999.99    | アクチュエーターによる     |

### 図
```mermaid
flowchart LR
    subgraph スライダー2領域[スライダー2領域（青背景）]
        direction LR
        A1[スライダー2 - 側ソフトリミット] --> A2[ストローク] --> A3[スライダー2 + 側ソフトリミット]
    end

    subgraph スライダー1領域[スライダー1領域（黄背景）]
        direction LR
        B1[スライダー1 - 側ソフトリミット] --> B2[ストローク] --> B3[スライダー1 + 側ソフトリミット]
    end

    基準[基準（工場出荷時の初期位置）] -->|位置関係| B1
    B3 -->|スライダー間最小距離| A1
    B2 -->|スライダー長| B3
    A2 -->|スライダー長| A3

    スライダー1座標["スライダー1の座標"]
    スライダー2座標["スライダー2の座標"]

    スライダー1座標 -.-> B2
    スライダー2座標 -.-> A2
```

**図の説明:**  
- 青背景はスライダー2の位置範囲（側ソフトリミットを含む）を示す。  
- 黄背景はスライダー1の位置範囲（側ソフトリミットを含む）を示す。  
- 基準は工場出荷時の初期位置を示し、スライダー1の側ソフトリミット位置に対応。  
- ストロークはスライダーの移動範囲を示し、スライダー間の最小距離も明示されている。  
</contents>