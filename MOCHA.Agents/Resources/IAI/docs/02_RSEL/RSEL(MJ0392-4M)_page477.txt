ファイル名: RSEL(MJ0392-4M)_page477

<overview>
このページは「7.2 直交座標系機能使用時の注意事項」に関する内容で構成されています。  
テキスト部分では、ロボットの手首の形態の境界である「特異点」について説明しています。  
図部分には、ロボット手首の形状と特異点（B軸原点）を示すイラストが配置されています。  
表はなく、文章と図による解説が中心です。
</overview>

<contents>
### テキスト
#### (2) 特異点
ロボットの手首の形態の境界を「特異点」と呼びます。

- 特異点近傍を通過、または特異点近傍へ位置決めする直交座標系指定のCP動作はできません。  
  特異点近傍を含むCP動作の場合は、48F「特異点エラー」が発生します。以下の方法で回避してください。

- 直交座標系を指定したCP動作（直線、円/円弧、スプライン移動、パス移動、直交座標上のジョグ移動など）で、始点・終点が特異点近傍である場合はロボットは動作できません。  
  特異点から離れた位置に変更、または各軸動作を使用してください。

- 直交座標系を指定したCP動作は経路上で特異点近傍を通過できません。  
  経路上で特異点（近傍を含む）を通過する場合は各軸動作を使用してください。

- 直交座標系指定のCP動作の現在位置と目標位置は同一形態である必要があります。  
  形態が異なる場合は経路上で特異点を通過する必要があり、495「形態エラー」が発生します。  
  CP動作の現在位置と目標位置は同一形態にしてください。

### 図
```mermaid
flowchart LR
    A[ロボット手首形状] --> B[特異点(B軸原点)]
    B -.-> C[特異点近傍通過不可]
```

**図の説明:**  
ロボット手首の形状の境界として特異点（B軸原点）が示されており、特異点近傍の通過や位置決めはCP動作でできないことを表しています。
</contents>