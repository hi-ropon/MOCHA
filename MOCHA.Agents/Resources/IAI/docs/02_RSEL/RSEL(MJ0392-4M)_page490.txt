ファイル名: RSEL(MJ0392-4M)_page490

<overview>
本ページは「7.2 直交座標系機能使用時の注意事項」に関する内容で構成されています。  
テキスト部分では、座標設定の注意点、出力ポート／グローバルフラグ番号の設定範囲と動作、エラー種別の説明が記載されています。  
図部分には、直交座標系のベース座標上の直方体の対角点を示す3次元座標軸と直方体のイメージ図が配置されています。  
表はなく、命令の使用例や詳細は別マニュアル参照を促す記述があります。
</overview>

<contents>
### テキスト
#### 7.2 直交座標系機能使用時の注意事項

- **X, Y, Z**  
  座標1と座標2にベース座標上の直方体の対角点となる値を設定してください。

- **出力ポート／グローバルフラグ No.**  
  300～599 / 4,000～6,999 / 600～899 の値を設定してください。  
  値が設定されているとき、直交型6軸ロボットツール先端がゾーン内進入中はON、ゾーン外はOFFになります。  
  ※システム出力ポート・フラグNo.（出力機能選択、ゾーンなど）と重複する番号は指定不可。

- **エラー種別**  
  - 0 = 進入時エラー処理無し  
  - 1 = 進入時メッセージレベルエラー  
  - 2 = 進入時動作解除レベルエラー  
  を設定してください。

SELプログラム内で簡易干渉チェックゾーンを操作するには、DFIF・SOIF・SEIF・GTIF命令を使用します。  
命令語の詳細は「SEL言語プログラミングマニュアル（MJ0224）」を参照してください。

### 図
```mermaid
flowchart TD
    subgraph 3D座標系
        Xb["Xb軸"]
        Yb["Yb軸"]
        Zb["Zb軸"]
    end
    subgraph 直方体
        座標1["座標1"]
        座標2["座標2"]
        直方体["直方体の対角点を指定"]
        チェックゾーン1["簡易干渉チェックゾーン1"]
    end

    Xb --> 直方体
    Yb --> 直方体
    Zb --> 直方体
    座標1 --- 直方体
    座標2 --- 直方体
    直方体 --- チェックゾーン1
```

**図の説明:**  
3次元ベース座標系（Xb, Yb, Zb軸）上に、座標1と座標2で指定される直方体の対角点があり、その範囲が簡易干渉チェックゾーン1として設定されることを示しています。
</contents>