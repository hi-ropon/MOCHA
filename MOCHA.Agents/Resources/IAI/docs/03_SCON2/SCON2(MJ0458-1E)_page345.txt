ファイル名: SCON2(MJ0458-1E)_page345

<overview>
このページは、リニアアブソ原点プリセット値（パラメーターNo.150）に関する説明で構成されています。  
主な構成は以下の通りです。  
・テキスト部分：パラメーターの概要、注意事項、原点位置変更時の計算式の説明  
・表部分：パラメーターNo.150の名称、単位、入力範囲、初期値を示した表  
・図部分：  
　1）擬似アブソ仕様のアクチュエーターの原点位置関係を示す模式図  
　2）シングルスライダー仕様の設定イメージ図  
図は位置関係や設定値のイメージを視覚的に示しています。
</overview>

<contents>
### テキスト
#### リニアアブソ原点プリセット値（パラメーターNo.150）
擬似アブソ仕様のアクチュエーターの場合、原点位置を設定します。  
基準（工場出荷時の初期位置）に対するそれぞれの位置関係を図で示しています。

##### 注意事項
- 設定前に工場出荷時の初期値を控えてください。  
- 工場出荷時の初期値を基準として設定してください。  
- 設定後は必ず低速で動作確認を行ってください。  
- 擬似アブソ仕様のアクチュエーターの場合、No.22（原点復帰オフセット量）およびNo.139（原点プリセット値）は無効となります。

##### 原点位置変更時の計算方法
1. シングルスライダー仕様の設定：  
パラメーター設定値 ＝ 工場出荷時の初期値（パラメーターNo.150）＋ 希望オフセット量

### 表
| No.  | 名称                     | 単位 | 入力範囲               | 工場出荷時の初期値           |
| :--- | :----------------------- | :--- | :--------------------- | :--------------------------- |
| 150  | リニアアブソ原点プリセット値 | mm   | -9,999.99～9,999.99    | アクチュエーターによる       |

### 図
#### 擬似アブソ仕様アクチュエーターの原点位置関係図
```mermaid
flowchart LR
    subgraph スライダー2 [スライダー2]
        direction LR
        A1[+側ソフトリミット] --> A2[スライダー2の座標]
        A2 --> A3[ストローク]
        A3 --> A4[-側ソフトリミット]
    end

    subgraph スライダー1 [スライダー1]
        direction LR
        B1[-側ソフトリミット] --> B2[スライダー1の座標]
        B2 --> B3[ストローク]
        B3 --> B4[+側ソフトリミット]
    end

    C[基準（工場出荷時の初期位置）] -->|スライダー長| D[スライダー1]
    C -->|スライダー間最小距離| E[スライダー2]
```
**図の説明:**  
- スライダー1とスライダー2の座標、ストローク、ソフトリミットの位置関係を示す。  
- 基準は工場出荷時の初期位置で、スライダー長やスライダー間最小距離が示されている。

#### シングルスライダー仕様の設定イメージ
```mermaid
flowchart LR
    工場出荷時の初期位置 --> 希望オフセット量 --> 希望原点位置
```
**図の説明:**  
- 工場出荷時の初期位置に希望オフセット量を加えた位置が希望原点位置となる。

</contents>