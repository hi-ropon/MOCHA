ファイル名: SCON2(MJ0458-1E)_page343

<overview>
このページはパラメーター設定に関する内容で構成されています。主に以下のセクションがあります。
- テキスト部分：パラメーターの説明や適用例、注意事項などの解説文
- 表部分：各パラメーターのNo.、名称、単位、入力範囲、工場出荷時の初期値をまとめた表
- グラフ部分：過負荷ロードレベル比の説明に関連したモーター温度変化とアラーム出力の関係を示すグラフ
</overview>

<contents>
### テキスト
#### サブネットマスク（パラメーターNo.141）
フィールドネットワーク（EtherNet/IP）専用のパラメーターです。詳細は別冊の「SCON2 フィールドネットワーク取扱説明書（MJ0469）」を参照してください。

#### デフォルトゲートウェイ（パラメーターNo.142）
フィールドネットワーク（EtherNet/IP）専用のパラメーターです。詳細は別冊の「SCON2 フィールドネットワーク取扱説明書（MJ0469）」を参照してください。

#### 過負荷ロードレベル比（パラメーターNo.143）
過負荷アラームとなるモーター推定上昇温度を100%とし、本パラメーターで設定した比率をモーター温度が超えたときにアラーム048「ドライバー過負荷警告」（メッセージレベル）を出力します。
本パラメーターの値を100%に設定すると判定を行いません。

【応用：予防保全機能】
グリースの枯渇や部品の消耗などで負荷状態が変化し、モーターの温度が上昇した場合に本パラメーターを設定しておくことで、エラーとして装置が停止する前に警告出力（＊BALM/＊OVLW/＊ALML信号）で知らせることができます。

### 図
```mermaid
flowchart TD
    A[負荷増] --> B[異常状態のモーター温度変化例]
    B --> C[アラーム出力]
    C -->|アラーム発生| D[ロボシリンダ停止]
    D --> E[アラーム復旧のためリセット操作必要]
    B --> F[警告出力]
    F -->|過負荷警告発生| G[ロボシリンダは継続して動作]
    B --> H[メンテナンスを実施]
    H --> I[警告出力後、メンテナンスを実施することで故障や不具合を防ぐことができる]

    subgraph モーター温度変化
        B
    end

    subgraph 時間軸
        J[時間]
    end
```

**図の説明:**
- 負荷増加によりモーター温度が異常に上昇
- 温度が設定値を超えるとアラーム出力し装置停止
- 警告出力は装置停止前に発生し、メンテナンスを促す

### 表
#### パラメーター一覧

| No.  | 名称               | 単位 | 入力範囲                 | 工場出荷時の初期値     |
| :--- | :----------------- | :--- | :----------------------- | :--------------------- |
| 141  | サブネットマスク   | ―    | 0.0.0.0～255.255.255.255 | 255.255.255.0          |
| 142  | デフォルトゲートウェイ | ―    | 0.0.0.0～255.255.255.255 | 0.0.0.0                |
| 143  | 過負荷ロードレベル比 | %    | 50～100                  | 100                    |
</contents>