ファイル名: SCON2(MJ0458-1E)_page178

<overview>
このページは「4.2 ポジショナーモードの運転」セクションの一部であり、ピッチ送り（相対移動＝インクリメンタル送り）について説明しています。構成は以下の通りです。
- テキスト部分：ピッチ送りの用途例、制御方法、注意事項の説明
- 図部分：ピッチ送りの動作イメージ図と速度・時間の関係グラフ、フローチャート的な動作説明
- 表部分：ピッチ送りの各位置における速度、加速度、減速度、動作種別、位置決め幅、停止位置指定方法の設定値一覧
</overview>

<contents>
### テキスト
#### 4.2 ポジショナーモードの運転
##### （3）ピッチ送り（相対移動＝インクリメンタル送り）
■用途例  
- ストッカの昇降、パレットの移動  
- マーキング工程のワーク送り　他

■制御方法  
1) ピッチ送りの制御方法は、ポジションテーブルの設定以外は位置決めと同じで、同一ポジションNo.の位置決めを繰り返す。  
2) ピッチ送りの場合、ポジションテーブルに設定した「位置」が移動するピッチとなる。  
「位置」の欄にピッチ幅（相対移動量＝インクリメンタル移動量）を設定する。  
運転指令が行われると現在の停止位置からポジションテーブルに設定した「位置」分の移動を行う。  
連続動作を行う場合は運転を繰返す。原点（座標値0）を基点としているため繰返しによる累積誤差は発生しない。

■注意  
- ピッチ送りでは、エンコーダーの最小分解能（リード/エンコーダーパルス数）以下の指令、および繰返し位置決め精度以下の指令を行わないこと。  
- 指令しても位置決め完了状態と同じ位置への指令となるため偏差は発生するが、正常な位置決め制御はできない。

### 図
```mermaid
flowchart LR
    A[① ポジション1、スタート信号入力（位置決め）] --> B[② ポジション1 100mmの位置へ移動完了]
    B --> C[③ ポジション2、スタート信号入力で25mm移動（ピッチ送り）]
    C --> D[④ ポジション2へのスタート信号入力のたびに25mmずつ繰返し移動]

    subgraph グラフ
        direction TB
        E[速度 250mm/s] --> F[時間 100]
        F --> G[停止状態]
    end
```

**図の説明:**  
- 左側イラストはストッカの昇降やマーキング工程のワーク送りの用途例を示す。  
- 右側グラフは速度と時間の関係を示し、250mm/sの速度で位置決めとピッチ送りを繰り返す動作を表現。  
- フローチャートはピッチ送りの動作手順を段階的に示している。

### 表
#### ピッチ送り設定例

| No. | 位置 [mm] | 速度 [mm/s] | 加速度 [G] | 減速度 [G] | 動作種別 (押付け力[%]) | 位置決め幅[mm]/押付け幅[mm] | 停止位置指定方法 |
| :-- | :-------- | :---------- | :--------- | :--------- | :--------------------- | :--------------------------- | :---------------- |
| 0   |           |             |            |            |                        |                              |                   |
| 1   | 100.00    | 250.00      | 0.30       | 0.30       | 位置決め               | 0.10                         | 0:絶対位置        |
| 2   | 25.00     | 250.00      | 0.30       | 0.30       | 位置決め               | 0.10                         | 1:相対位置        |

（注）ポジションNo.2がピッチ送りの設定である。
</contents>