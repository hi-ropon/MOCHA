ファイル名: SCON2(MJ0458-1E)_page167

<overview>
このページは「ポジショナーモードの運転」に関する説明で構成されています。ページ上部にテキスト説明があり、その下に多回転仕様のロータリーアクチュエーターのインデックスモードの例を示す2つの円グラフ（図）が配置されています。ページ下部には注意事項のテキストがあります。表はなく、図は円グラフ形式で角度の設定値を示しています。
</overview>

<contents>
### テキスト
#### 多回転仕様のロータリーアクチュエーターがインデックスモードの例
- 2つの円グラフで、設定値のゾーン境界の角度を示している。
- 左図はゾーン境界＋側が70度、ゾーン境界－側が315度。
- 右図はゾーン境界＋側が315度、ゾーン境界－側が70度。

#### 注意
- 本信号は、原点復帰完了後に座標系が確立してから有効になるため、電源投入しただけでは出力しない。

### 図
```mermaid
flowchart LR
    subgraph 左図
        A0(0度)
        A70(70度)
        A315(315度)
        A0 -->|時計回り| A70
        A70 -->|時計回り| A315
        A315 -->|時計回り| A0
        note1[設定値\nゾーン境界＋側：70度\nゾーン境界－側：315度]
    end

    subgraph 右図
        B0(0度)
        B70(70度)
        B315(315度)
        B0 -->|反時計回り| B315
        B315 -->|反時計回り| B70
        B70 -->|反時計回り| B0
        note2[設定値\nゾーン境界＋側：315度\nゾーン境界－側：70度]
    end
```
</contents>