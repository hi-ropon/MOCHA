ファイル名: RCON(MJ0384-8D)_page489

<overview>
このページは「6.2.9 フル直値2モードの割付け」に関する内容で構成されています。  
テキスト部分では、フル直値2モードの割付けの概要と設定単位の説明があり、  
表部分ではPLC出力の軸制御信号に対する各通信プロトコル（CC-Link、DeviceNet、PROFIBUS-DP、PROFINET-IO）でのアドレス割付けが示されています。  
また、図部分では16ビットワードのビット割付けが視覚的に表現されています。
</overview>

<contents>
### テキスト
#### 6.2.9 フル直値2モードの割付け
フル直値2モードの割付けは以下の通りです。  
押付け時の電流制限値、加速度、減速度および速度はアクチュエーターの仕様範囲内で設定し、  
目標位置データ、ゾーン境界値はソフトストロークの範囲内で設定してください。

**設定単位**  
- 電流制限値 = 1%  
- 加減速度 = 0.01 G  
- 速度 = 1.0 mm/s  
- 目標位置・位置決め幅・ゾーン境界値± = 1/100 mm

### 表
#### PLC出力＝軸制御信号のアドレス割付け

| CC-Link / CC-Link IE Field / CC-Link IE TSN | DeviceNet | PROFIBUS-DP, EtherNet/IP, EtherCAT | PROFINET-IO | 内容 |
| :--- | :--- | :--- | :--- | :--- |
| RWw m+0 | n+0 | n+0 (上位バイト), n+1 (下位バイト) | p+0 | 目標位置 (L) (符号付整数) |
| RWw m+1 | n+1 | n+2, n+3 |  | 目標位置 (H) (符号付整数) |
| RWw m+2 | n+2 | n+4, n+5 |  | 位置決め幅 (L) |
| RWw m+3 | n+3 | n+6, n+7 |  | 位置決め幅 (H) |
| RWw m+4 | n+4 | n+8, n+9 | p+1 | 速度 (L) |
| RWw m+5 | n+5 | n+10, n+11 |  | 速度 (H) |
| RWw m+6 | n+6 | n+12, n+13 |  | ゾーン境界値＋ (L) (符号付整数) |
| RWw m+7 | n+7 | n+14, n+15 |  | ゾーン境界値＋ (H) (符号付整数) |

### 図
```mermaid
flowchart TD
    subgraph 1ワード=16ビット
        direction LR
        bit15[15b] --> bit14[14b] --> bit13[13b] --> bit12[12b] --> bit11[11b] --> bit10[10b] --> bit9[9b] --> bit8[8b] --> bit7[7b] --> bit6[6b] --> bit5[5b] --> bit4[4b] --> bit3[3b] --> bit2[2b] --> bit1[1b] --> bit0[0b]
    end

    A1[目標位置 (L) (符号付整数)] --> A2[目標位置 (H) (符号付整数)] --> A3[位置決め幅 (L)] --> A4[位置決め幅 (H)] --> A5[速度 (L)] --> A6[速度 (H)] --> A7[ゾーン境界値＋ (L) (符号付整数)] --> A8[ゾーン境界値＋ (H) (符号付整数)]
```
</contents>