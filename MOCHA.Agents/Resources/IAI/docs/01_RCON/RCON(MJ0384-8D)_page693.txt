ファイル名: RCON(MJ0384-8D)_page693

<overview>
このページは「8.6 エレシリンダーパラメーターの詳細」に関する内容で構成されています。  
主に以下のセクションで構成されています。  
・テキスト部分：エレシリンダーの動作範囲調整に関する説明、注意事項  
・表部分：パラメーターNo.1の詳細（名称、単位、入力範囲、初期値）を示す表  
・図部分：エレシリンダーのストローク設定例を示すイラストと寸法表示  
</overview>

<contents>
### テキスト
#### 8.6 エレシリンダーパラメーターの詳細
（1）動作範囲調整（パラメーターNo.1）

- システムに合わせてエレシリンダーの動作範囲を調整可能。  
- 最小設定単位は0.01mm。  
- 使用したいストロークを設定する。コントローラーが自動的に0.30mm加算し、動作範囲を制御・監視する。  

（設定例）  
使用ストロークを0～80mmに設定したい場合は、パラメーターNo.1を「80.00mm」に変更する。

#### 注意
- エレシリンダーの可動範囲内で設定すること。  
- 最大ストロークを超える数値を設定すると、前進側のメカストッパーに衝突し、エレシリンダーやワーク・周辺機器を破損する恐れがある。

### 表
| No. | 名称           | 単位 | 入力範囲      | 工場出荷時の初期値 |
| :-- | :------------- | :--- | :------------ | :----------------- |
| 1   | 動作範囲調整   | mm   | 0～9999.69    | 最大ストローク     |

### 図
```mermaid
flowchart LR
    A[使用ストローク設定] --> B[パラメーターNo.1を設定]
    B --> C[コントローラーが0.30mm加算]
    C --> D[動作範囲制御・監視]

    subgraph ストローク寸法例
        direction LR
        E[0.30mm] --> F[80.00mm (使用ストローク)] --> G[0.30mm]
    end
```

**図の説明:**  
エレシリンダーのストローク設定例を示し、使用ストローク80.00mmの前後に0.30mmの余裕を持たせて動作範囲を制御していることを表している。
</contents>