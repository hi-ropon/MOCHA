ファイル名: RCON(MJ0384-8D)_page568

<overview>
本ページは「入出力信号のタイミングと運転手順」に関する内容で構成されています。テキスト部分では位置決め動作時の注意点について説明しています。図部分には位置決め動作のタイミングチャートが2つ示されており、指令ポジションからアクチュエーター移動までの信号の変化を表しています。表部分にはティーチングツールで使用されるポジションテーブルが掲載されています。
</overview>

<contents>
### テキスト
#### 位置決め動作時の注意点
位置決め動作中に相対座標のポジションNo.を選択入力しスタート入力を行うと、最初のポジションに相対移動量を加えた位置へ移動します。相対移動量がマイナスの場合は最初のポジションから減じた位置へ移動します。

例）ポジション1へ移動中にポジション2のスタート入力を行うと、原点から40mmの位置に行きます。

また、位置決め動作中に相対座標のポジションNo.へのスタート入力を複数回行うと、最初のポジションに「相対移動量×回数」を加えた位置へ移動します。

例）ポジション1へ移動中にポジション2のスタート入力を2回行うと、原点から50mmの位置に行きます。

### 図
```mermaid
flowchart TD
    subgraph タイミングチャート1
        direction TB
        A1[指令ポジション: ポジション1 → ポジション2]
        B1[位置決めスタート (CSTR)]
        C1[位置決め完了 (PEND)]
        D1[完了ポジション: ポジション2]
        E1[移動中 (MOVE)]
        F1[速度]
        G1[アクチュエーター移動]
        H1[原点からの位置: 40]
    end

    subgraph タイミングチャート2
        direction TB
        A2[指令ポジション: ポジション1 → ポジション2]
        B2[位置決めスタート (CSTR) 複数回]
        C2[位置決め完了 (PEND)]
        D2[完了ポジション: ポジション2]
        E2[移動中 (MOVE)]
        F2[速度]
        G2[アクチュエーター移動]
        H2[原点からの位置: 50]
    end
```

### 表
#### ティーチングツールでのポジションテーブル
| No. | ポジション | ソクド |
| :-- | :--------- | :----- |
| 0   | ＊         | ＊     |
| 1   | 30        | 100    |
| 2   | 10        | 100    |
| ... | ...        | ...    |
</contents>