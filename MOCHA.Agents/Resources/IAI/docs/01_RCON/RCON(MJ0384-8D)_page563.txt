ファイル名: RCON(MJ0384-8D)_page563

<overview>
本ページは「6.5.5 移動中の速度変更」に関する説明で構成されています。  
テキスト部分では、移動中に速度を変えることの意義や使用例が述べられています。  
表部分では、速度変更に必要なポジションテーブルの例が示されています。  
グラフ部分では、速度変更のタイミングや信号の動作手順を示すタイミングチャートが掲載されています。
</overview>

<contents>
### テキスト
#### 6.5.5 移動中の速度変更
1動作で複数の速度制御が可能です。移動中、ある地点から速度を遅くしたり、また速くしたりすることができます。ただし速度を変化させるごとにポジションデータが必要です。  
搬送物の材質が柔らかい場合や、ビンなどの転倒しやすい形状のワークで停止時に振動や衝撃を与えたくない場合に本機能が有効です。

（例）ポジション2（原点より300mm）に位置決めする場合、途中のポジション1（原点より200mm）までは200mm/sの速度で、以降は20mm/sの速度で移動させます。

### 表
#### ポジションテーブルの例

| No. | 位置[mm] | 速度[mm/s] | 加速度[G] | 減速度[G] | 押付け[%] | 位置決め幅[mm] | コメント |
| :-- | :------- | :--------- | :-------- | :-------- | :-------- | :------------- | :-------- |
| 0   | *        | *          | *         | *         | *         | *              |           |
| 1   | 200.00   | 200.00     | 0.30      | 0.30      | 0         | 20.00          |           |
| 2   | 300.00   | 20.00      | 0.30      | 0.30      | 0         | 0.10           |           |

### 図
#### 移動中の速度変更タイミングチャート

```mermaid
flowchart TD
    subgraph ポジション指令
        direction LR
        A1[ポジション1] --> A2[ポジション2]
    end

    subgraph 位置決めスタート(CSTR)
        direction TB
        B1 --> B2
    end

    subgraph 位置決め完了(PEND)
        direction TB
        C1 --> C2
    end

    subgraph 完了ポジション
        direction LR
        D1[ポジション1] --> D2[ポジション2]
    end

    subgraph 移動中(MOVE)
        direction LR
        E1 --> E2
    end

    subgraph 速度
        direction LR
        F1[0] --> F2[200mm/s] --> F3[20mm/s]
    end

    subgraph アクチュエーター移動
        direction LR
        G1 --> G2 --> G3
    end

    B1 -.->|T1※| D1
    B2 -.->|T1| D2
    F2 -->|180mm| G2
    F3 -->|ポジション1(200mm)| G3
    G3 -->|ポジション2(300mm)|
```

**図の説明:**  
- ポジション1（200mm）までは200mm/sで移動し、その後20mm/sに速度を落としてポジション2（300mm）に到達する。  
- 位置決め開始(CSTR)から完了(PEND)までの信号の変化と速度変化のタイミングを示している。  
- T1は上位コントローラーのスキャンタイムを考慮し、0ms以上となるように設定する必要がある。
</contents>