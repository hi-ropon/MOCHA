ファイル名: RCON(MJ0384-8D)_page589

<overview>
本ページは「7.3 節電機能」に関する説明で構成されています。テキスト部分では、自動サーボOFF機能およびフルサーボ機能の概要と動作説明が記載されています。表部分では、アクチュエーターの状態に応じた節電機能の設定条件がまとめられています。グラフや図は含まれていません。
</overview>

<contents>
### テキスト
#### 7.3 節電機能

【自動サーボOFFおよびフルサーボ機能】
アクチュエーター停止中の電力消費量を低減するため、自動サーボOFF（全モーター仕様設定可能）およびフルサーボ機能（パルスモーター仕様限定）を搭載しています。安全かつ運転上の支障がないように使用してください。

- 自動サーボOFF機能は、位置決め完了後、一定時間経過後に自動的にサーボOFFします。次の位置決めスタート信号CSTRが指令されると自動的にサーボONし、位置決め動作を実行します。停止時の保持電流が流れないため電力消費量を削減可能。位置決め完了からサーボOFFまでの時間は3種類設定可能。

- フルサーボ機能は、停止時電流が多いパルスモーターを停止時にもサーボ制御し電力消費量を削減。パルスモーターは停止時の微振動を抑え完全停止するため一定の電流を流す。エンコーダー値が目標位置から±4カウントの範囲のずれでは復帰しないが、フルサーボ機能有効時は1カウントのずれでも復帰動作を行う。RCP6シリーズはフルサーボ機能と同様に1カウントのずれでも復帰動作を行うため、RCP6ではフルサーボ機能は使用不可。

節電機能はアクチュエーターの状態により、パラメーターNo.53またはポジションテーブルの「停止モード」のどちらかの設定が有効になる。

### 表
| 状態 | 設定 |
| :--- | :--- |
| 原点復帰完了で待機中（目標位置へ位置決めは行っていない状態） | パラメーターNo.53に設定した値で節電機能実行（ポジションNo.の停止モード設定は無効） |
| 電源投入後、サーボON状態で待機中（目標位置へ位置決めは行っていない状態） | パラメーターNo.53に設定した値で節電機能実行（ポジションNo.の停止モード設定は無効） |
| ポジションテーブルに設定している目標位置に位置決め完了した状態で待機中 | 各ポジションNo.の「停止モード」に設定した値で節電機能実行（パラメーターNo.53の設定値は無効） |
</contents>