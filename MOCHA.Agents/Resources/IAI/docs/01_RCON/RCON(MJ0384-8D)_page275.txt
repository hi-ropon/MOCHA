ファイル名: RCON(MJ0384-8D)_page275

<overview>
本ページは「4.5 システムI/Oの配線」についての説明で構成されています。  
主に以下のセクションで構成されています。  
- テキスト部分：停止回路の配線方法の説明  
- 図部分：ティーチングボックスとゲートウェイユニットの接続イメージ図および回路例の配線図  
- 表部分はなく、注釈としてRCONゲートウェイの仕様による回路の違いが記載されている  
グラフは含まれていません。
</overview>

<contents>
### テキスト
#### 4.5 システムI/Oの配線
##### 4.5.1 停止回路の配線
停止回路の配線方法を示しています。

1. ティーチングボックスの停止入力だけを使用し、アクチュエーターを動作させる場合、システムI/Oコネクターを出荷時の状態のままゲートウェイユニットに挿入します。

##### 注釈
- RCONゲートウェイの仕様により回路が異なります。  
  - RCON-GW: SIOコネクターに何も接続されていない場合、コントローラー内部でS1とS2が短絡されます。  
  - RCON-GWG: SIOコネクターに何も接続されていない場合、コントローラー内部でS1とS2は短絡しません。短絡させる場合は、SIOコネクターに付属のダミープラグDP-5を接続してください。

### 図
#### 配線イメージ
```mermaid
flowchart LR
    TB[ティーチングボックス\n停止スイッチは有効です。] -->|ケーブル| SIO[SIOコネクター]
    SIO --> RCON[RCON]
    SIO -.->|システムI/Oコネクター| RCON
```

#### 回路例
```mermaid
flowchart LR
    subgraph ティーチングボックス
        STOPB[停止スイッチ STOPB]
        STOPA[停止スイッチ STOPA]
    end

    subgraph ゲートウェイユニット
        SIO8["SIOコネクター 8"]
        SIO5["SIOコネクター 5\n※1"]
        SIO2["システムI/Oコネクター 2"]
        SIO1["システムI/Oコネクター 1"]
        SIO6["システムI/Oコネクター 6\n停止用回路専用24V"]
        SIO7["システムI/Oコネクター 7"]
    end

    subgraph ドライバーユニット
        Driver[駆動源遮断回路]
    end

    STOPB --> SIO8
    STOPA --> SIO5
    SIO2 -->|S1| SIO1
    SIO6 --> Driver
    SIO7 --> Driver

    %% 出荷時配線済みの線
    S1 -.-> SIO2
    S2 -.-> SIO1
    STOP+ -.-> SIO6
    STOP- -.-> SIO7
```

### 注釈
| 項目 | 内容 |
| :--- | :--- |
| RCON-GW | SIOコネクターに何も接続されていない場合、コントローラー内部でS1とS2が短絡される |
| RCON-GWG | SIOコネクターに何も接続されていない場合、コントローラー内部でS1とS2は短絡されない。短絡させる場合は付属のダミープラグDP-5を接続する必要がある |

</contents>