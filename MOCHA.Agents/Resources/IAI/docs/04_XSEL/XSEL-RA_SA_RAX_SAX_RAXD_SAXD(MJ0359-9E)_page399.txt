ファイル名: XSEL-RA_SA_RAX_SAX_RAXD_SAXD(MJ0359-9E)_page399

<overview>
本ページは「6.5.29 ティーチングツールを接続しなくてもグローバル整数変数をモニターしたい」という項目で構成されています。  
テキスト部分では、グローバル整数変数の内容をパネルウィンドウに表示する方法と表示範囲の説明があり、  
表部分ではパラメーターNo.49とNo.50の設定内容が示されています。  
グラフ部分は7セグメント表示の例が2つあり、数値の表示方法を視覚的に説明しています。
</overview>

<contents>
### テキスト
#### 6.5.29 ティーチングツールを接続しなくてもグローバル整数変数をモニターしたい
グローバル整数変数の内容をパネルウィンドウに表示させることが可能です。  
表示できる範囲は正の整数値で1～999までです。  
999を超えた値は「U---」の表示となります。

パラメーター入力後、フラッシュROM書込み、ソフトウェアリセット（再立上げ）を行うと、  
「レディーステータス表示」・「プログラム実行中No.表示」の代わりに、グローバル整数変数の内容を表示します。  
向かって一番左側のセグメントは「U」を表示します。

### 表
| パラメーターNo. | 設定値                         | 内容                         |
| :-------------- | :----------------------------- | :--------------------------- |
| その他のパラメーターNo.49 | 2                             |                             |
| その他のパラメーターNo.50 | モニタリングするグローバル整数変数No. |                             |

### 図（7セグメント表示例）
```mermaid
flowchart LR
    subgraph 表示例1
        A1["  _  "] --> A2[" | | "]
        A2 --> A3[" |_| "]
        note1["左端は'U'表示"]
    end
    subgraph 表示例2
        B1["  _  "] --> B2[" | | "]
        B2 --> B3[" |_| "]
    end
```

**図の説明:**  
- 7セグメント表示の赤線部分が点灯している状態を示している。  
- 1つ目の例は「U001」の表示例。  
- 2つ目の例は「0999」の表示例。  
- 「U」は999を超えた値の表示を示す。  
</contents>