ファイル名: SCON2_FIELDNETOWORK(MJ0469-3A)_page307

<overview>
本ページは「3.5 入出力信号の制御と機能」についての説明で構成されています。テキスト部分では押付け方向指定（DIR）信号の意味と動作条件について詳細に解説しています。図部分には押付け動作の速度と位置の関係を示す2つのグラフがあり、押付け方向がプラス方向の場合とマイナス方向の場合の動作の違いを視覚的に説明しています。表は含まれていません。
</overview>

<contents>
### テキスト
#### 押付け方向指定（DIR）PLC出力信号
押付けを行う方向を指定する信号です。

- 信号をONにすると目標位置に位置決め幅を加算した値に向かって押付けを行う。
- 信号をOFFにすると目標位置から位置決め幅を減算した値に向かって押付けを行う。
- 通常位置決め動作の場合は本信号は無効。
- 設定タイミングは「4.2.2 ハーフ直値モードでの運転【2】」または「4.2.3 フル直値モードでの運転【2】」を参照。

##### 押付け動作の方向指定例
1) 始点からプラス方向に押付け動作を行う場合  
2) 始点からマイナス方向に押付け動作を行う場合

- 始点から目標位置に向かって座標値が増える方向に押付ける場合は押付け方向プラス（＋）に設定。
- 座標値が減る方向に押付ける場合は押付け方向マイナス（－）に設定。
- 押付け方向の設定を誤ると正しい動作ができず、（位置決め幅×2）の距離だけ始点側で押付け動作してしまうため注意が必要。

### 図
```mermaid
flowchart LR
    subgraph プラス方向押付け動作
        A[始点] -->|押付け速度で加速| B[設定速度到達]
        B --> C[正しい動作]
        C --> D[目標位置（押付け動作開始位置）]
        D --> E[押付け動作最大移動位置]
        E --> F[現在位置]
        B -.->|DIRをマイナス（－）にした場合| G[誤動作]
    end

    subgraph マイナス方向押付け動作
        H[押付け動作最大移動位置] --> I[目標位置（押付け動作開始位置）]
        I --> J[正しい動作]
        J --> K[設定速度到達]
        K --> L[現在位置]
        L -.->|DIRをプラス（＋）にした場合| M[誤動作]
    end
```

**図の説明:**  
- 上段は始点からプラス方向に押付け動作を行う場合の速度と位置の関係を示す。  
- 下段は始点からマイナス方向に押付け動作を行う場合の速度と位置の関係を示す。  
- それぞれの図中に、DIR信号を逆に設定した場合の誤動作の例も示されている。

</contents>